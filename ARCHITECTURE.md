# Aegis EMR — Architecture (the LIVE system)

> **This is the single maintained reference for how the live Aegis EMR is wired** (repo `compact-emr-work`,
> app at `emr.flatratenexus.com`). For the FRN drafter *internals* and the public website lane, see
> `flatratenexus-project/ARCHITECTURE.md` — this doc owns the EMR + the seam between them, and points there
> rather than duplicating drafter logic.
>
> **HARD RULE — update this file in the SAME commit** that adds/flips a flag, changes a model ID, adds or
> removes an AI brain, changes the case/data flow, or retires a design decision. A decision is not "done"
> until it's recorded here (especially in the **RETIRED / SUPERSEDED** log). Prose-only memory drifts; this
> file is the antidote. See `MEMORY.md` operating-defaults for the standing rule.
>
> **Last reviewed:** 2026-06-22 — body-quality holds are now advisory/editable AND forwardable: `/halt` preserves the produced draft (persists `artifactTxtS3Key` + advances `currentVersion`), `needs_rn_decision` is editor-editable, the new `needs_rn_decision → physician_review` edge lets the RN forward a hand-fixed letter (never a re-draft-only trap), sign-off still refuses parked cases (gates on answer content) (see §6b).

---

## 1. System flow (case lifecycle)

```
Intake (Jotform → webhook Lambda + hourly jotform-sweep backstop)
  → S3 raw docs (PHI bucket)
  → OCR / vision read  (per-page Sonnet vision worker; legacy Textract)  [worker, async]
  → chart-extract      (Anthropic Sonnet → structured facts, SC list, problems)  [Lambda, async]
  → viability route-picker  (deriveAiViability → Sonnet)  ← ONE BRAIN (card + Ask-Aegis + drafter framing)
                            computed OFF the request path (API Lambda async self-invoke) → persisted → card READS
  → drafter            (Fargate task, SQS FIFO draft-job, Opus)  [worker, async]
  → physician QA / sign-off  (review page, sign-off attestations)
  → render             (DOCX/PDF render Lambda)
  → deliver            (Stripe payment → token mint → Gmail/SES signed-PDF autodelivery)
```

AWS primitive per stage: **API** = monolithic Node/Express Lambda behind API Gateway (HTTP API, **hard 29s
cap** — see §6 latency rule). **Workers** = Lambdas (OCR, chart-extract, jotform-ingest/sweep, doctor-pack)
+ the **Fargate drafter** (scale-to-zero, SQS FIFO). **Store** = RDS Postgres 16 (+ pgvector for Ask-Aegis
corpus), S3 (PHI + artifacts). **Auth** = Cognito (TOTP MFA). **Secrets** = Secrets Manager (read by
FRIENDLY NAME, never partial-ARN — see INCIDENTS). Deploy = GH Actions → CDK (§6).

---

## 2. Data-model invariants (load-bearing)

- `cases.version` — optimistic-lock counter, `+1` on every PATCH / state transition. The Phase-1
  migration enforces this with a BEFORE-UPDATE `compact_emr_touch_version()` trigger (`NEW.version =
  OLD.version + 1`) on a hand-listed set of Phase-1 tables.
- **`draft_jobs.version` is SEMANTIC, NOT a lock counter — it is EXCLUDED from the touch-version
  trigger (2026-06-29 fix).** `version` here = the letter draft number (v5 = the 5th nexus-letter
  attempt); concurrency is enforced by `draft_jobs_case_id_in_flight_uq` (partial unique) + terminal-state
  guards, not by an arithmetic counter. The Phase-1 migration WRONGLY attached `compact_emr_touch_version`
  to `draft_jobs`, so every `/progress` heartbeat UPDATE inflated `version` (Dick: created v5, ended v27 →
  `Case.currentVersion=26`) and version-derived S3 artifact keys (`drafter-artifacts/<caseId>/v<N>/...`)
  404'd. Migration `20260801000000_drop_draft_jobs_touch_version` drops the trigger. **Drafter version
  authority = `letter-current.ts`'s stored S3 keys / `LetterRevision` rows, NEVER arithmetic** — application
  code sets `draft_jobs.version` and `Case.currentVersion` explicitly. (See §7 SUPERSEDED + INCIDENTS
  2026-06-29.)
- `claimedCondition` (primary) vs `claimedConditions[]` (the clustered set CDS/drafter read when non-empty).
  PATCH keeps them in sync on a single-condition claim (`routes/cases.ts`).
- **`ai_viability_plan_json` + `ai_viability_plan_hash`** (2026-06-19) — the persisted route-picker plan.
  `hash` = sha256 of the picker inputs (claimed, granted-SC, problems, events, guidance, veteran statement).
  Written hash-guarded by `deriveAiViability`; **NULLED on input-affecting PATCH** (`routes/cases.ts`
  `PLAN_INPUT_FIELDS`). The card reads/recomputes; Ask-Aegis narrates it (§4). Plan stamps `schemaVersion`
  + `inputClaimed` so a stale-shape / wrong-condition blob is refused, not mis-rendered.
- `caseViabilityBand` / `caseViabilityAnchor` — the older static viability snapshot (written only-when-null).
  Now superseded on the Overview by the route-picker plan (see §7).
- `framingStampSource` / `viabilityStampSource` / `cdsStampSource` — `'manual'` (staff-set, immutable to
  auto-refresh) vs `'derived'` (machine, restampable).
- DraftJob — SQS FIFO + heartbeat; never deploy the drafter mid-draft (queue must be idle).

---

## 3. The AI brains — which is LIVE, model, prompt canon

| Brain | Purpose | Provider / Model | Flag (current) | Prompt canon → vendored into EMR |
|---|---|---|---|---|
| Per-page vision OCR | read scanned/handwritten pages | Anthropic, Sonnet | `CLAUDE_VISION_SCANNED_PAGES` | worker |
| Chart-extract | structured chart facts, SC list, problems | Anthropic **direct**, Sonnet 4-6 | `CHART_AUTOFILL` / `CHART_EXTRACT_FULLREAD` | chart-extract Lambda (shares backend/src) |
| ↳ **Granted-SC authority (deterministic)** | the granted-SC anchor is NOT trusted to the stochastic LLM — `rating-decision-grants.ts` regex-parses every "service connection for X is granted [at N%]" recital and MERGES into the extractor's `raw` before grounding/dedup (Sonnet supplements, can't drop the anchor). 2026-06-20, 3rd recurrence fix; PROVEN 4/4 vs Sonnet 0/4 on Hackworth. | deterministic (no model) | always on | `backend/src/services/rating-decision-grants.ts` → chart-extract Lambda |
| **Viability route-picker** | card + Ask-Aegis grounding + drafter framing (ONE brain) | Anthropic, `claude-sonnet-4-6` (`AI_ROUTE_PICKER_MODEL`) | `AI_ROUTE_PICKER_ENABLED` = true | FRN `app/services/aiRoutePicker.js` → `backend/src/vendor/aiRoutePicker.cjs` (**sha-pinned** by `ai-route-picker-pin.test.ts`) |
| Drafter | the nexus letter | Anthropic, Opus | drafter image tag (cdk.json) | FRN `app/services/*` → Fargate ECR image |
| Ask-Aegis advisory | RN/physician case Q&A | **Bedrock**, Opus 4.6 | always on | FRN `app/config/advisory/rn_advisory_system_prompt.md` → `backend/src/advisory/systemPrompt.ts` |
| Opus sanity-impression | pre/post-draft gut-check | Anthropic, Opus | (cached per case+stage) | `backend/src/services/sanity-impression.ts` |
| **SOAP-note Overview** | the RN's calm AI-synthesized S/O/A/P lead on the Overview | Anthropic **direct**, Sonnet 4-6 (`SOAP_NOTE_MODEL`) | always on | `backend/src/services/soap-overview.ts` (`buildSoapNote`); POST `/cases/:id/soap-overview`. **Context is SERVER-derived** via `soap-context-assembler.ts` (`assembleSoapContextForCase`) for BOTH the async precompute and the sync read (write==read fingerprint, 2026-06-22) — the FE body no longer feeds the fingerprint. Coverage note uses the shared `loadExtractionCoverageForCase` (same % as the chart chip). |
| ↳ **Overview chip (verdict)** | the one go/no-go chip + the deterministic detail card | deterministic (no model) | always on | `frontend/src/lib/caseReadinessVerdict.ts`. The chip is a **PROJECTION of the route-picker band** (`routePickerBandToVerdict`) when a plan is ready; the deterministic engine is **fallback-only** (drives the chip only when `routePickerViability == null`) and can never contradict a ready plan (band-vs-core conflict → visible disagreement, not a flip). |

**⚠️ Auto-vendored prompt trap:** `systemPrompt.ts` and `aiRoutePicker.cjs` are GENERATED from the FRN source
(`vendor-advisory-prompt.cjs` / the anchor vendor copy). **Hand-edits to the vendored files are reverted on
the next vendor run.** Always edit the FRN canonical source, then re-vendor. The api-stack copies the whole
vendor tree to `<task>/anchor-vendor` at deploy.

**One brain, four surfaces (2026-06-19, chip added 2026-06-22):** the route-picker plan drives (a) the
Overview viability/SOAP card (`deriveAiViability`), (b) Ask-Aegis viability answers (narrated from the
PERSISTED plan — no second LLM call on the 29s path; `advisory/aiViabilityPlanBlock.ts`), (c) the drafter
framing gate, and (d) the **Overview verdict CHIP** (projected from the band via `routePickerBandToVerdict`,
so the chip can never contradict the SOAP note it sits above). The picker DECIDES; the card visualizes,
Ask-Aegis explains, the chip summarizes. Confidence is subordinated to confirmed chart gate elements (no
over-sell). Hard excludes (reverse-causation / pyramiding / wrong-direction) carry to all surfaces. The
chip→note agreement is pinned cross-module by `oneBrainChip.agreement.test.ts` (`routePickerBandToVerdict`
go/no-go === `soap-action-map.ts planViabilityToAction` go/no-go).

---

## 4. Flags (current state)

| Flag | Where | Default | OFF behavior |
|---|---|---|---|
| `AI_ROUTE_PICKER_ENABLED` | api + drafter | `true` | card → static viability; drafter → legacy framing gate |
| `EMR_CASE_VIABILITY_ENABLED` | api | `true` | no static viability snapshot stamp |
| `DIRECT_SC_VIABILITY_ENABLED` | api | `true` | secondary-only viability axis |
| `BRIDGE_ANCHOR_ENABLED` | api | `true` | — |
| `DRAFT_READINESS_GATE` | api | RETIRED 2026-06-29 (env no longer read; gate code removed) | — |
| `DOCTOR_PACK_GROUNDED_PAGES` | api | `on` | ungrounded page select |
| `DOCTOR_PACK_LINKED_COVER` | api | **`off`** (dark) | legacy plain-text cover, no link-map |
| `GUIDED_REVISION_ENABLED` | api | `true` | — |
| `ANCHOR_MECHANISM_GATE` | drafter | `true` | anchor candidates unfiltered |
| `DRAFTER_GATE2_ENABLED` | drafter | `true` | no pre-draft dx/event halt |
| `PLAN_VALIDITY_PARK` | drafter | **`on`** (2026-06-30; was advisory) | Stage-0.5a plan-validity gate stays ADVISORY (surface, never park) |
| `AEGIS_VIABILITY_GROUNDING` | advisory retrieve | **off** (unset) | legacy static viability facts block (off in prod) |

---

## 5. Latency rule (hard constraint — read before adding any LLM call to the API)

The API Lambda is behind API Gateway with a **hard 29s cap** (Lambda Timeout=29). Any synchronous LLM call
on a request path that runs past 29s **kills the function before its own catch/fail-open runs** → silent
failure (the SOAP "thinks for minutes then nothing" + the original double-call timeout). Rules:
- Never make TWO sequential LLM calls on one request (the SOAP card was fixed by sharing one query).
- Bound any single on-request Anthropic client to **timeout ≤ ~22s, maxRetries 0** so it fails-open LOUDLY
  inside the window (`deriveAiViability`).
- Prefer **compute-once-persist-then-read**: `deriveAiViability` short-circuits to the persisted plan when
  inputs are unchanged (no LLM on a cold Lambda); Ask-Aegis reads the persisted plan rather than recompute.
- **Async self-invoke pattern (the viability picker, 2026-06-19):** a compute that needs >~20s can't run on
  the synchronous request. The API Lambda invokes ITSELF (`InvocationType:'Event'`, event
  `{__recomputeViability,caseId}` handled in `placeholder-lambda.ts` before Express) — the fresh invocation
  owns the whole 29s window alone, so the picker runs at timeout 26s (vs the 22s sync cap) and completes +
  persists. The GET `/viability-card` is READ-ONLY (`deriveAiViability` `compute:false`): persisted plan or
  null, and fires the async compute on a miss. The FE polls until the plan lands. Needs `SELF_FUNCTION_NAME`
  env + `grantInvoke(self)` (api-stack). Reuse this pattern for any future >20s on-request compute.
- **Gap:** no CloudWatch alarm on API Lambda Errors / Duration≥29000 yet (silent-fail backstop — TODO).

---

## 6. Deploy mechanics

- Push to `main` → GH Actions (`deploy-staging.yml` / `deploy-prod.yml`) → `cdk deploy --all` + a dedicated
  **`compact-emr-staging-prisma-migrate-deploy` CodeBuild** (idempotent migrate runner, NOT raw
  `prisma migrate deploy` — tolerates the out-of-order timestamp).
- **Ordering note:** migrate CodeBuild currently runs *after* `cdk deploy` → a brief window of
  code-without-columns. Benign today only because every new-column read/write is fail-open. Reordering
  migrate-before-deploy for additive migrations is an open improvement.
- Drafter image: built from a CLEAN worktree, pushed to ECR, tag pinned in `infra/cdk.json`
  (`drafter_image_tag`); flip a drafter env flag via task-def re-register (runtime, no rebuild) — but a
  later `cdk deploy` converges it back unless baked into `drafter-stack.ts`.
- CI gates: `migrate:check`, `migrate:diff-check`, tsc, vitest.

---

## 6b. 🛡 DRAFT RELIABILITY MECHANISMS (2026-06-20 hardening — the "stop failing silently" pass)
- **DRAFT-JOB INTEGRITY — drop the version-inflation trigger + cancel/watcher take the case OFF 'drafting' (2026-06-29, EMR-only; Michael Dick).** TWO fixes. **(1) SYSTEMIC — `draft_jobs.version` inflation.** The Phase-1 `compact_emr_touch_version()` BEFORE-UPDATE trigger (`NEW.version = OLD.version + 1`) was WRONGLY attached to `draft_jobs`, whose `version` is the SEMANTIC letter number, not a lock counter — so every `/progress` heartbeat inflated it (Dick: created v5, ended v27 → `Case.currentVersion=26`) and version-derived S3 artifact keys 404'd. Migration `20260801000000_drop_draft_jobs_touch_version` DROPs the trigger (metadata-only, safe live); `schema.prisma` unchanged (version was never `@default(autoincrement())`). Drafter version authority = `letter-current.ts` stored keys, never arithmetic (see §2 invariant). NOTE: this does NOT repair already-corrupted `currentVersion` rows (they survive on the read path via `LetterRevision` stored keys) — a separate read-only audit+realign is OWED. Guard test `draft-jobs-no-touch-version-trigger.test.ts` statically scans the migrations dir and asserts no `*_touch_version` trigger governs `draft_jobs` (the existing mock-DB suites only simulate the trigger on `cases`, never `draft_jobs` — that is why CI was blind). **(2) CANCEL/STRANDED — take the case off 'drafting'.** `POST /cases/:id/draft-jobs/:jobId/cancel` (`routes/drafter.ts`) flipped the `DraftJob` terminal but never reset `Case.status`, so the Cases list read "Drafting" forever with no in-flight job. The cancel `$transaction` now ALSO sets `status: needs_rn_decision`, `operatorState: 'paused'`, a friendly `operatorMessage`, `runComplete: false`, `version +1` — mirroring the stuck-job-watcher terminal-case write; the newest job is now terminal so the in-flight gate (`CaseDetailPage.tsx` keys on newest-job state) clears and redraft (POST `/draft`) stays available. The **stuck-job-watcher** (`lambdas/stuck-job-watcher.ts`) gains a RECONCILE pass that runs every sweep (even when no stale job exists — Dick's job was already terminal): cases at `status='drafting'` + `operatorState != 'paused'` (don't clobber the sweep's own OpsHeldPanel handling) + has a job but none `queued|running` get the same paused/non-drafting transition; idempotent (once off 'drafting' it no longer matches). Tests: `drafter-cancel-resets-status.test.ts`, reconcile block in `stuck-job-watcher.test.ts`. **(3) FE PANEL ROUTING — fixed (the cancelled case now shows the calm OpsHeld panel, not the dx card).** `caseTopPanels.ts` carries an `isParkedWithoutHalt` predicate (`status==='needs_rn_decision'` + `operatorState==='paused'` + NO halt payload + NO halted job): a cancelled / watcher-reconciled case matches it → renders `OpsHeldPanel` ("Re-run full draft"), and is EXCLUDED from `isGate2Halt` so the dx-verification card ("override / change diagnosis / records are in") never shows for a cancel. A REAL dx-halt still carries `haltPayloadJson` and/or a `halted` job, so `hasHaltPayload`/`hasHaltedJob` keep genuine halts on `Gate2HaltPanel` even when `operatorState` reads 'paused'. Both halt signals are derived from the **NEWEST** draft job only (`deriveLatestHaltSignals` in `caseTopPanels.ts`, consumed in `CaseDetailPage.tsx`) — NOT a `.some()`/`.find()` over all jobs — so a resume→cancel sequence (older job halted+payload, newest job failed) can't let a stale prior halt re-route the cancelled case onto the dx card. Tests: cancelled→OpsHeld + real-halt→Gate2 + the resume→cancel residual in `caseTopPanels.test.ts`. **Remaining follow-up: none material** — the latest-job derivation closes the stale-prior-halt corner (see INCIDENTS 2026-06-29).
- **SOAP CARD — auto-refresh on finish + Ask-Aegis is a CONSIDERATION, not the default (2026-06-29, EMR-only; Dr. Kasky two polish refinements).** TWO changes on the Case-Overview/SOAP card. **(1) AUTO-REFRESH the SOAP note when the async precompute finishes — no hard refresh (`SoapOverviewCard.tsx` + `case-viability.ts` route + `case-viability.ts` api client).** Dr. Kasky had to hard-refresh to see the final note; navigating away/back did nothing because the main `soapQ` had `staleTime:10min` and served the cached PROVISIONAL fallback brief on remount. Fix has two halves: (a) `refetchOnMount:'always'` on `soapQ` so a remount always re-reads (a $0 serve-stored-first cache hit once the real note is persisted); (b) a SEPARATE cheap **poll query** (`soapPollQ`) enabled ONLY while the served note is provisional (`note.fallback === true` — THE provisional→final signal: the 110s async `precomputeSoapNoteForCase` flips it to `fallback:false` when it lands the grounded note), polling every 15s, capped at 20 polls (~5 min) then a manual "Check for the full summary" affordance. The poll calls the route with a NEW **`pollOnly:true`** flag → a $0 STATUS CHECK: the route serves the persisted real note the instant the precompute lands it (`decideServeStored`), otherwise returns `{data:null, generating:true}` WITHOUT a sync Sonnet generate and WITHOUT re-firing the recompute (honoring the documented warming-window re-bill trap + the chip-wobble recompute-storm freeze). When the real note lands, `soapPollQ` folds it into the main `soapQ` cache (`setQueryData`) → provisional flips false → the poll disables itself. **NO `SOAP_NOTE_SCHEMA_VERSION` bump** — this is fetch/poll wiring only, no note-shape/grounding change. **(2) ASK-AEGIS IS A CONSIDERATION, NOT THE DEFAULT** (Dr. Kasky "I never want Ask Aegis to be the default answer like they feel like they have to"). Every Ask-Aegis mention in the RN next-action / plan text is reframed from the imperative "run an Ask-Aegis check" to an OPTIONAL second read ("an Ask-Aegis check can help if useful"); the PRIMARY moves (confirm the records are already in the chart, ask the veteran one question, check the presumptive path) lead. Three sources: `caseReadinessVerdict.ts` `nextActionFor` (CLIENT-SIDE interim/tentative plan — not cached → takes effect IMMEDIATELY, the text Dr. Kasky actually saw); `soap-overview.ts` `actionToPlanSentence` + the SOAP tool `plan` description + the SYSTEM prompt (LLM prose — softened going forward). **NO schema bump here either** — reasoning: the interim text is client-side (no cache); the LLM prompt already listed Ask-Aegis as one option (not a reflex), so softening it improves new/regenerated notes without a fleet-wide regeneration; the v28 bump (2026-06-28, deployed) is already regenerating pre-v28 notes on next open, so the softened prompt reaches them naturally — a SECOND mass regeneration is not justified for a non-harmful phrasing nudge. Locked test tokens preserved (`Ask-Aegis`, `RN WORK ORDER`, `REVIEWS and SIGNS`, `ask the doctor what he thinks`). Tests: pollOnly $0-status-check + never-bills/never-re-fires (`soap-overview-route.test.ts`), Ask-Aegis-optional (`caseReadinessVerdict.test.ts` + `soap-determinism-sticky.test.ts`). backend+frontend tsc 0; SOAP/viability/headline/verdict suites green.
- **SOAP AUTO-REFRESH — 2nd attempt: the poll now covers ALL provisional states, not just `note.fallback` (2026-06-29, EMR-only; Dr. Kasky hit it again on Marcus Bennett).** The first auto-refresh (above) gated `soapPollQ` ONLY on `note.fallback === true` (the model-truncation brief). But the note Marcus saw was a REAL (`fallback:false`) note written **while chart extraction was still in_progress** — its prose hedged "…the content of those opinions is not fully extracted in the available pages." That state never polled → it persisted until a HARD REFRESH re-read the note the completed extraction had since produced. Root cause confirmed: `fallback` does NOT distinguish a provisional-on-incomplete-chart note from the final note (both are `fallback:false`); the distinguishing signal is the **fingerprint** — the provisional note was written under the incomplete-chart ctx hash, and once extraction completes the live ctx hash drifts off it. Fix, three parts: **(1) `SoapOverviewCard.tsx` — widen `soapProvisional`** to fire the poll when ANY of: `note.fallback` OR the chart is still being analyzed (`coverage.chartAnalysis.state`/`status === 'in_progress'`, the self-resolving case — TERMINAL `failed`/`incomplete`/whole-file-gap are excluded, they need a manual re-extraction not a poll) OR the served note is `stale` (drifted inputs). **(2) `case-viability.ts` pollOnly — land ONLY on a fingerprint-CURRENT note:** `decideServeStored` returns a stored note whenever it is current-shape + non-fallback REGARDLESS of fingerprint drift (`refresh` flags the drift). The poll now serves it as final ONLY when `decision.refresh === false`; a drifted stored note (the note built on the pre-completion chart) reports `generating:true` so the poll keeps waiting for the refreshed note instead of swapping the stale one in and disabling itself. Still $0 — no model call, no recompute fired in pollOnly. **(3) `SoapOverviewCard.tsx` — kick the single drift-recompute on chart completion:** pollOnly deliberately fires no recompute, so on its own it would wait forever for a refreshed note nothing produces. A ref-guarded effect invalidates the MAIN `soapQ` ONCE on the `in_progress → settled` edge → that normal re-open hits serve-stored-first, sees the fingerprint drift, and fires the ONE off-request `fireRecomputeViability` that writes the complete, current-fingerprint note (which the poll then folds in). Fires once per edge, never per coverage tick (no recompute storm). navigate-away-back stays covered by `soapQ`'s `refetchOnMount:'always'`. **NO `SOAP_NOTE_SCHEMA_VERSION` bump** — fetch/poll wiring only. Tests: drifted-stored-note → generating:true (`soap-overview-route.test.ts`), poll-fires-in-incomplete-extraction + complete-chart-does-not-poll (`SoapOverviewCard.test.tsx`). backend+frontend tsc 0.
- **GATE-2 HALT — always-present inline "Change diagnosis & re-draft" + the switch button now patches the chart too (2026-06-29, EMR-only; Michael Dick).** The dx-verification `Gate2HaltPanel` offered a dx change ONLY when the drafter surfaced a `switchProposal` (the "Switch to {dx}" button). When the drafter buried the better-fit dx in prose with no proposal (Michael Dick), the RN had no inline way to re-aim the letter. Fix (`Gate2HaltPanel.tsx`): **(1)** an ALWAYS-PRESENT free-text dx input (pre-filled with `switchProposal.dx` when present, else empty) + a **"Change diagnosis & re-draft"** button → one `changeDx` mutation that, IN ORDER, `patchCase(id, {version, claimedCondition})` (PATCH `/cases/:id` — backend syncs `claimedConditions[]` + invalidates the plan) THEN `postDraft(id, {rnDecision:{switchToCondition, reason}})` (re-aims the run + writes the `DraftDecision` audit row). The existing `window.prompt` reason gate is kept (deliberate confirm + audit before the ~$15 run); optimistic-concurrency `ConflictError` → `onChanged()` reload + retry prompt (the page's existing pattern). **(2)** FIXED a pre-existing bug — the old "Switch to {dx}" button re-aimed the letter via `postDraft` but NEVER `patchCase`d, so the chart dx stayed stale (chart showed the old dx while the letter drafted the new one). It now routes through the SAME `changeDx` path so chart + letter agree. Override / records-are-in / pause unchanged beside it; body-quality park branch untouched. Tests: `Gate2HaltPanelDxChange.test.tsx` (no-proposal inline change patchCase-then-postDraft + order, reason-gate honored, switch-button-also-patches, input pre-fill). frontend tsc 0; `BodyQualityHoldCard.test.tsx` green (no regression).
- **PRE-DRAFT HALT — no false "produced a letter / View PDF" affordance (2026-06-29, EMR-only; Michael Dick CLM-A158C00C07).** A Gate-2 dx-verification halt fired PRE-DRAFT — NO letter artifact was ever rendered (all `DraftJob` artifact keys null, `drafter-artifacts/<caseId>/` empty in S3, `Case.currentVersion=0`, 0 letter_revisions). Yet `OpsHeldPanel` showed *"This draft did not finish — but it produced a letter"* with **Open letter editor / Send to doctor / View PDF** — all of which 404'd: `GET /cases/:id/letter` runs `resolveCurrentForRead(0)`, which returns null for `currentVersion < 1` (and the S3-truth walk found nothing), then threw `HttpError(404,'no_letter')` at `letter.ts:411` **silently**. Root cause: the claim page's `viewableLetterJob` predicate (`CaseDetailPage.tsx`) decided "a letter exists" from the halted job's terminal STATE alone (`hasPdfKey || state==='done' || state==='failed'`) — so a keyless pre-draft-halt job matched and every letter affordance lit up. **Fix:** the predicate is hoisted into a pure, unit-tested `resolveViewableLetterJob(draftJobs, currentVersion)` (`frontend/src/lib/viewableLetter.ts`) that treats a letter as viewable ONLY when a real, RESOLVABLE artifact exists — a present `.pdf` key OR `currentVersion >= 1` (the EXACT mirror of `resolveCurrent`'s `currentVersion < 1 → null` gate, what `GET /letter` can actually serve). This is the SINGLE signal feeding every letter affordance (View PDF / Open editor / Send to doctor / RN editor-entry / Delivery verify), so all surfaces fix in lockstep. **Preserved (no regression):** a halt that DID produce a letter — `/halt` persisted `artifactTxtS3Key` + advanced `currentVersion` (§6b above, task #96), or the stuck-watcher race where the job's key column was lost but the artifact rendered + the pointer advanced — still has `currentVersion >= 1`, so it still correctly offers the editor/PDF (the no-block-draft rule). When NO artifact exists, `OpsHeldPanel`'s honest `hasLetter=false` variant already renders ("The run stopped and did not produce a letter") and suppresses the letter buttons. **FAIL-LOUD:** the `letter.ts:411` no-letter 404 now logs `no_artifact caseId=… currentVersion=…` (was silent) so a missing-letter GET is greppable in CloudWatch. Tests: `frontend/src/lib/viewableLetter.test.ts` (RED→GREEN: pre-draft halt v0/null-keys → not viewable; halt-with-letter + .pdf key + stuck-watcher race → still viewable) + a `letter-routes.test.ts` case asserting the `no_artifact` warn fires on a v0/no-artifact GET. backend+frontend tsc 0; OpsHeldPanel/caseTopPanels/letter-routes suites green. Task #215.
- **CASE-OVERVIEW / SOAP CHIP STABILITY — decided once, sticks (2026-06-28, EMR-only; Dr. Kasky "the chip keeps changing color on its own, amber→green→amber").** The chip read was already correctly locked to the persisted `note.action` (`soapChip.ts` ← `soap_overviews.result_json`), but that `action` was (1) non-deterministically generated, (2) force-overwritten on nearly every recompute, and (3) re-rolled by a recompute STORM during drafting. Four durable fixes, in leverage order: **(1) temperature:0** on BOTH decision LLM calls — the route-picker band (`ai-viability.ts`, was 0.5) and the SOAP note (`soap-overview.ts`, was the SDK default 1.0) — so the band/action is a deterministic function of the inputs (pinned by a grep test). **(2) STICKY VERDICT (keystone):** `reconcileStickyAction(fresh, stored, grounded)` (`soap-overview.ts`, pure) + a `reconcile` hook on `getOrBuildSoapNote` (called AFTER generate, BEFORE persist) — `precomputeSoapNoteForCase` passes it so an UNGROUNDED recompute (no authoritative route-picker band — e.g. the inputHash drifted while drafting) REGENERATES PROSE FREELY but does NOT overwrite the chip-bearing `action`; only a GROUNDED recompute (a genuine band change) flips it. **(3) RECOMPUTE-STORM FREEZE:** while `status='drafting'` the drafter rewrites the Case row every few seconds, drifting the route-picker inputHash (`ai-viability.ts:220`, hashed over drafter-mutated claimed/sc/problems/events/guidance/veteranStatement) → the plan reads 'none' → the SOAP rebuilds ungrounded → a re-fired recompute re-rolls everything. The four passive auto-fires of `fireRecomputeViability` (both `/soap-overview` + the `/viability-card` GET in `case-viability.ts`) now SKIP when `status='drafting'` (chosen over narrowing the hash inputs, which would also stop a REAL clinical change from invalidating the plan); the serve-stored-first branch still serves the persisted grounded note for $0, and the next open after drafting completes recomputes normally. The EXPLICIT `/compute` endpoint (RN Retry) is NOT gated. **(4) TITLE HUMANIZER + AMBER = WORK-ORDER:** `humanizeFraming` (`soapHeadline.ts`) rewrites a leaked raw enum token ("direct"→"Direct", "in_service_onset"→"In Service Onset") to Title Case while leaving real prose untouched; and the amber/needs-review guidance at all three sources (`soap-overview.ts` `actionToPlanSentence('physician_review')` + the SOAP tool/SYSTEM prompt; `caseReadinessVerdict.ts` `nextActionFor`) is reframed from "route to a physician" into concrete RN-OWNED next steps (confirm the records are actually in the chart — most "missing" facts are just un-parsed — run a named Ask-Aegis check on the mechanism, check the presumptive path; escalate to the team lead ONLY when the engine genuinely can't resolve it) — physicians REVIEW and SIGN (~2%); the RN + engine make the go/no-go. Locked verdict phrases (reconcile the anchor / AI check also flags / more records / drafter) preserved. Tests: `soap-determinism-sticky.test.ts` (temp grep-pin + 6 sticky cases + amber prompt pins), `soapHeadline.humanize.test.ts`, amber-reframe block in `caseReadinessVerdict.test.ts`. backend+frontend tsc 0; SOAP/viability/headline/verdict suites green. Supersedes the 2026-06-27 chip-lock (`soapChip.ts`) — that locked the chip to `note.action` but did not make `note.action` itself stable. **(2026-06-29 follow-up — `SOAP_NOTE_SCHEMA_VERSION` 27→28):** the reframe rewrote the PROMPT but left every PRE-existing cached SOAP note (keyed by `input_hash`+`schema_version`) intact, so a hard-refresh re-served the old "route to a physician" plan (Dr. Kasky hit exactly this on Sanderson). Bumping the schema version invalidates all v27 notes → each case REGENERATES its plan with the reframed prompt on next open. One-time cost: every case re-runs one SOAP LLM call on its next open. Also reframed the degraded-fallback assessment line ("needs a physician read" → "open the chart to review, then decide the path").
- **GOOGLE ADS OFFLINE CONVERSIONS — repointed to the child account's OWN action (2026-06-29; $4,281/30d spent, 0 conversions tracked).** `backend/src/services/google-ads-conversions.ts` uploaded (Stripe $50-payment webhook → `uploadReviewConversion`) to conversion action `7664449375` "Nexus Letter Intake" — which exists ONLY on the MANAGER (MCC 9775308437), NOT on child account 3654279964. The child is `conversion_tracking_setting=MANAGED_BY_SELF` (counts only its OWN actions), so every upload landed NOWHERE — the Ads API showed only a manual-CSV client, no API-client row ever. FIX = repointed `CONVERSION_ACTION_ID` → `7635957826` "Intake Completed" (the child's own ENABLED `CONVERTED_LEAD` action; 11 manual CSV uploads already succeeded against it). `AD_ACCOUNT_ID`=3654279964 was already correct; account stays self-managed (do NOT switch to cross-account). **gclid capture is code-sound on our side** (`website/intake.html` `?gclid`→`s1_gclid` hidden field [+`localStorage` fallback] → `jotform-ingest/handler.py` writes the FULL answer set → `internal-worker.ts` persists `Intake.rawAnswersJson` verbatim → `extractGclidForCase` matches `s1_gclid`/`gclid` live at payment time). **There is NO `cases.gclid` column** — gclid is read live from `rawAnswersJson`; a query against a column will falsely look broken. The only loss point is the Jotform hidden-field config (Ads/Jotform side). **VERIFY LANDING via the API Lambda CloudWatch log** (`[google-ads] … requestId=… perDestStatus=…` from the Data Manager `events:ingest` 200) — NOT `offline_conversion_upload_client_summary`, which only tracks the CLASSIC Ads API and shows NOTHING for our Data Manager ingests (false-negative trap). Supersedes `flatratenexus-project/handoffs/running/20260627_google_ads_conversion_tracking.md` (wrong action ID). **ENHANCED-CONVERSIONS HEDGE REMOVED — gclid-only (2026-06-30, privacy/HIPAA; SUPERSEDES the 2026-06-29 hedge).** The upload no longer sends the veteran's email (even hashed) to Google. A SHA-256 hashed email is a match identifier Google resolves back to the person, so it tied a named individual to "completed a VA-disability nexus-letter intake" — a health-context identifier barred by Google's own sensitive-data policy; Google was also 400-rejecting the `userData` shape (the code self-healed to gclid-only). So gclid-only is both the safe and the working path. `extractEmailForCase`, `hashedEmail`, the `createHash` import, and the `userData`/`userIdentifiers`/`emailAddress` payload + the reject-and-retry branch are all DELETED from `google-ads-conversions.ts`. (Prior hedge, now retired: it attached `userData.userIdentifiers[].emailAddress` as a fail-safe gclid-match backup.) Destination account confirmed = child `3654279964` (`operatingAccount.accountId`); no MCC `login-customer-id` anywhere — the action repoint was sufficient, the destination was never the problem.
- **CITATION ENRICHER — physician grounded-NCBI add-verified-citation (2026-06-24, EMR-only).** A physician, on ANY letter, requests medical citations; the system does an async grounded NCBI retrieval (vendored `backend/src/vendor/citationFallback.cjs` — every PMID from esearch, killer stat = verbatim abstract substring, retraction reject), the physician PREVIEWS candidates, and on APPLY the selected PMIDs are SERVER-SIDE re-verified and deterministically inserted into §VIII (+ §VI grounding) as a new letter version via the existing PUT-version path. 3 routes on `letter.ts`: `POST .../citations/enrich` (202+jobId, physician-only), `GET .../enrich/:jobId` (poll), `POST .../citations/apply`. Scratchpad table `citation_enrich_jobs` (migration `20260722000000`). THE KEYSTONE SAFETY: `diffCitationsSanctioned(before, after, verifiedPmids)` lets a net-new citation through ONLY if its PMID is in the server-re-verified set — the fabrication guard (`diffCitations`/422 `citation_invented`, unchanged for normal surgical/guided paths) is SATISFIED by verification, never bypassed (verification is server-side, never a client flag; insertion is deterministic). Egress: the API Lambda reaches NCBI via the existing NAT (keyless 3/s for now). Tasks #132/#134-144.
- **GUIDED-REVISION PROPOSER RESILIENCE (2026-06-24).** The surgical/guided proposer used to 500 on a long/dense passage or a transient Anthropic overload, surfacing the generic "could not be generated". Now: SDK `maxRetries:4` + 60s timeout (exponential backoff on 429/5xx/529/timeout/connection), and a typed `ProposerUnavailableError` → HTTP 422 `proposal_unavailable` with a specific detail (`model_unavailable` / `passage_too_complex` (max_tokens truncation) / `no_change_proposed`) + a matching actionable UI message instead of the generic fallback. Holding-lock / citation-integrity / §VII role-gate untouched. Task #145.
- **ASK AEGIS — longer client timeout + +50% answer length (2026-06-25, EMR-side).** RNs were hitting a false "could not reach the server (timeout of 30000ms exceeded)" even though the answer completed (visible on navigate-away/back) → they resent. Root: ONE global axios timeout (`client.ts` `apiClient`, 30s) shared by every call. Fix: global default 30s→**45s** + an optional per-request `{timeout}` on the apiGet/apiPost/apiPut/apiPatch/apiDelete wrappers (backward-compatible), and the two genuinely-long synchronous AI ops get their own ceiling — **askAdvisory 60s** (Bedrock/Opus grounded RAG) and **proposeGuidedRevision 70s** (the backend surgical proposer has a 60s SDK timeout w/ maxRetries:4, so the client must outlast it). Separately, advisory answers were truncating at the max-token cap ("cut off at the length limit — ask me to continue") so `ADVISORY_MAX_TOKENS` (`backend/src/advisory/bedrockClient.ts`) raised **1024→1536** (×1.5, advisory-only — drafter/chart-extract token budgets untouched; compiled constant → needs the backend redeploy). tsc 0 both; advisory + client + letter-api suites green.
- **RELIABLE §VIII CITATION FLOOR — etiology-gated grounded backfill (2026-06-25, FRN drafter image `956dcd8-routepicker`; Ryan "we reliably need citations made").** Spring (occupational acne) shipped 1 §VIII citation. Root cause: the grounded fallback was capped at 3 (one per A1/A2/A3 slot) so it could never reach the floor of 4. FIX (FRN commits `08f8196` + fileset-gate `956dcd8`): `retrieveGroundedAnchors` top-up pass + broadened occupational/military query lanes + per-request `NCBI_API_KEY`; a fail-open POST-DRAFT `backfillSectionViiiCitations` (run-letter-pipeline render try-block, after fold, before the case-law strip + persist + render, render-parity-safe, byte-identical no-op at ≥floor, `v<N>_citation_backfill.json` audit sidecar). **PCP-nexus-writer QA fix (the key safety):** an etiology-discriminating fit gate + off-etiology denylist (rejects a PAPA-syndrome/PSTPIP1 paper for an occupational-acne letter) + a FIT BRAKE — append only GOOD fits, leave §VIII short rather than pad to 4 (the Apolito over-citation anti-pattern). Citations NEVER halt/fail (the floor stays advisory, off the unbypassable list); the advisory now names the count + points the physician to the Citation Adder. `citationFallback.js` gated into the c0 fileset (now output-determining via the backfill). c0 record grade **A** ($8.06, lumbar at-floor → backfill no-ops → byte-identical = no regression). QA: AWS (deploy blast radius safe, latency fine in the 45-min SQS visibility window) + architect (fail-open/parity/no-pad confirmed) + PCP-writer (citation quality). DURABLE FOLLOW-UPS logged: curate an occupational_acne library folder (don't lean on live NCBI for a common pathway); re-vendor citationFallback.cjs into the EMR enricher for symmetric etiology gating; CloudWatch metric-filter on backfill error-no-op (fail-loud on a dead NCBI key).
- **CITATION ENRICHER 4-bug fix + NCBI API key wired (2026-06-25, EMR-side).** The physician "Add Verified Citation" tool: (1) inserted §VIII entries now match the house numbered format ("N. Authors. Title. Journal. Year;Vol(Issue):Pages. PMID: id." via full_citation); (2) condition-search inserts real §VIII references instead of a generic §VI "these articles support the opinion" sentence; (3) the grounded NCBI retrieval (vendored citationFallback.cjs) hardened — 12s timeout, 3 retries, exp backoff honoring Retry-After on 429, clear actionable empty message; (4) **THE KEYSTONE:** a guided/surgical revision can now REFERENCE a citation already in the letter's §VIII — the integrity guard (letter.ts ~727) builds the allowed-PMID set from EVERY PMID in the current letter and calls diffCitationsSanctioned, so a §VIII PMID woven into §VI prose is a legal cross-reference, NOT `citation_invented`; a PMID nowhere in the letter or a net-new author-year/stat STILL rejects (fabrication guard intact). **NCBI_API_KEY** wired into both consumers via Secrets Manager `frn/ncbi-api-key` (ARN ...-c8E88t): the api Lambda (`api-stack.ts`, `{{resolve:secretsmanager:...}}` env) for the enricher, and the Fargate drafter task-def (`drafter-stack.ts`, `ecs.Secret.fromSecretsManager`) for the §VIII backfill — raises NCBI 3→10 req/s + reliability; keyless fallback if absent. Imported by complete ARN (partial-ARN footgun avoided, INCIDENTS 2026-06-05). Blast radius: api Lambda `[~]` in-place code+env update, drafter task-def new revision; no replacement, no destructive migration, NCBI egress over the existing NAT. Paired FRN drafter change (the reliable §VIII citation floor + etiology-gated backfill) ships in the NEXT drafter image.
- **AGGRAVATION-PRONG FORCING RETIRED + §VII scaffolding-leak fix (2026-06-25, FRN drafter image `794ec6e-routepicker`; Ryan "we dont push aggravation at all, let drafter decide, that was old thinking").** A c0 caught the regression: on a DIRECT lumbar case the drafter bolted on a 3.306 aggravation prong AND leaked instruction scaffolding into the bolded §VII opinion. Two root causes — (1) the AGGRAVATION-FIRST forcing machinery (trigger-detection MUST-plead-both, dual-prong unbypassable) still live despite the 2026-06-22 walk-back; (2) the 508-char leaked holding overran the `[^*]{0,400}` cap in every §VII opinion-bounds regex → every §VII guard failed open. FIX (FRN, 3 commits `ab0807a`/`d804e4e`/`794ec6e`): forcing removed on DIRECT **and** SECONDARY (trigger BINDING→advisory; the 5 dual-prong rule_ids demoted critical→warning+physician_decision and off the unbypassable list; BVA-judge/CFR-auditor role prompts no longer flag single-prong; claude.js single-prong-by-default; aggravation-first→evidence-led) — the drafter now DECIDES the theory and aggravation is OPT-IN (physician surgical edit; the holding lock already allows causation→aggravation). KEPT: a deterministic §VII scaffolding guard (opinionSentence + Phase-6.4 backstop), §VII bounds widened 400→800 lock-step (6 regexes), the 3.310(b) pre-aggravation-baseline doctrine, and ALL safety/substance guards (PHI, fabricated-PMID, locked blocks, §VII substance, the aggravation-ONLY inverse guard stays unbypassable, reg-conflation). Also bundled in this EMR push (no drafter dependency): the persisted-route-picker-plan **stamp** (`ai-viability-plan-stamp.ts` → `drafter.ts` + `draft-auto-rerun.ts` + `drafter-bundle.ts`) so the drafter honors the SOAP theory the RN saw (Spring fix, EMR half); render-parity halt now reads "Section VII, paragraph 2" not a raw offset (`letterLocation.ts` + `OpsHeldPanel`); and the substantive grader hints moved off the RN "Fix before sending" into the physician "Considerations before signing" (`OpsHeldPanel` + `PhysicianLetterReadyPanel` + `PhysicianMobileReviewPage`). c0 record grade **A** ($8.22); super-detailed QA: no safety guard weakened, full suite no NEW reds.
- **VETERAN STATEMENT FED TO BOTH THE THEORY BRAIN AND THE DRAFTED PROSE (2026-06-25, FRN drafter image `7c2c61c-routepicker`; Ryan "the SOAP/theory brain must appreciate customer input on equivocal arguments, and that must be fed to the drafter").** A write/read path mismatch left the drafter blind to the veteran's own statement: the drafter writes it to `chart/index.json` → `claim_intake_summary.veteran_statement` (`drafter-worker.js:370`), but (a) `aiRoutePicker.js:119` read only `veteran_statement` / `caseFraming.veteran_statement` (neither populated — `caseFraming.v1` has no such field), so the **drafter's route-picker** never weighed the veteran's argument, and (b) `claude.js` had **no veteran-statement block at all**, so the drafted prose never saw it. Fix (both additive + fail-open, BYTE-IDENTICAL prompt when no statement): (1) `aiRoutePicker.js` reads `claim_intake_summary.veteran_statement` first in its fallback chain; (2) `claude.js _draftLetterInner` injects a veteran-statement block framed `authority="none" trust="untrusted-input"` (verbatim route-picker framing) + two guards ("ground only in record+library", "do not change the lead theory"). The statement shapes emphasis / pre-empts counterarguments but cannot establish a fact, dx, or rating (deterministic rails unchanged). **The EMR `ai-viability.ts` card was NEVER blind** (it reads `c.veteranStatement` straight from the `Case` column) — so this aligns the DRAFT with what the SOAP/viability card already shows the RN (one brain). The `.cjs` re-vendor + pin-test sha bump (`1527fb41…`) ships via this EMR push to keep the SYSTEM/TOOL pin aligned (card behavior unchanged). c0 record-mode grade **A** ($8.36, supportable lumbar path byte-identical = no regression); 6/6 + 15/15 in-process checks prove the block fires only when a statement is present. FRN commit `7c2c61c`.
- **BVA TURNED OFF FOR REAL + grounded citation fallback (2026-06-25, FRN drafter image `44b5d07-routepicker`).** Two drafter fixes in one cutover. (1) **BVA off in the framing brain:** the 739184f "BVA removal" only neutered the prose (`getBVAContext→''`); `framingGate.js` — the gate that actually PICKS the theory — was still loading `bva_secondary_pairs.json`, stamping a "BVA PAIR-LEVEL PRIORS" table into the prompt, defaulting to "secondary per atlas +20pt vs direct", and ranking anchors by Board grant rate. Proven on Wickel (CLM-EB5ABA7D79): a strong DIRECT in-service MDD case was drafted SECONDARY-to-tinnitus, its framing.json rejecting direct on "atlas-best 62% / shrunk_grant 65.4%". Now framingGate loads NO atlas, stamps NO Board stats, has NO secondary default; theory follows clinical evidence + the route-picker's prior; anchors ranked by real mechanism then rated_pct. `claude.js` `bvaLine` win-rate numbers neutered. (2) **Citation fallback fires on thin coverage:** `assessCoverage` fires the grounded NCBI retrieval when matched folders lack a mechanism anchor / <4 on-topic PMIDs (not only zero candidates — a loose broad-folder match no longer suppresses it); relaxed on-topic gate + per-mechanism A3 cascade; advisory §VIII PMID floor (fail-open). c0 record-mode grade **A** ($7.78). MORNING DECISION owed to Ryan: flip `AI_ROUTE_PICKER_ENABLED=true` (task-def env, no rebuild) to make framing DEFER to the route-picker = one brain (task #72) + deterministic Wickel fix.
- **NO HARD FAILS — fixable body-quality residuals COMPLETE as editable advisories, not parks (2026-06-24, FRN drafter image `30e1a43-routepicker`; Ryan "no hard fails unless wrong-vet-obvious; lean advisory").** Supersedes the blanket body-quality park below. The FRN `draftBodyQualityGate.terminalDispositionFor()` now splits a residual material finding (one that survived self-heal) 3 ways: a true **HARD-STOP** — ONLY `letter_self_undercut` or a load-bearing fabricated PMID — keeps the `/halt` park (reasonCode stays `verify_error`) with an explicit *"do NOT send; re-run once; if it recurs contact Dr. Kasky"* script; **everything else COMPLETES** via a normal `/complete` (`operatorState:'ready_with_notes'`, `runComplete:true`) with the findings folded into `gradeSidecar.targeted_revision_hints` and `ship_recommendation:'revise'` (real grade kept — a non-empty hints list already forces `shipAsIs=false` in the UI). The case lands in `rn_review` (editable + forwardable via `PhysicianLetterReadyPanel`, which renders the grade chip + top-3 hints) — never a dead park. `render_parity`: a COSMETIC divergence (whitespace/punct/ligature only, similarity≥0.995, `renderParity.isCosmeticDivergence`) is no longer `fail(7)` — it completes with a no-penalty PDF-formatting advisory; a real content divergence still fails closed. No EMR-side change was needed (the rn_review surface already reads `gradeSidecarJson.targeted_revision_hints`). c0 record-mode grade A- (no content change). The `/halt` `body_quality_critical` path below is retained for the rare hard-stop but is now seldom hit.
- **Granted-SC = deterministic authority** (see §6 chart-extract row): the load-bearing anchor never depends on the LLM alone. `rating-decision-grants.ts`. History: [[project_chart_extraction_method_history]] — Sonnet STILL reads the whole chart (A/B winner); this is a narrow deterministic FLOOR under one templated field, NOT a revert.
- **Stale-extraction auto-refresh** (`EXTRACTOR_VERSION` in `chart-build-state.ts`): a chart-extract code fix only helps NEW runs, and the reprocess cost-safety gate used to skip re-extraction whenever the DOC set was unchanged (`already_extracted_no_changes`) — so a deployed fix never reached open cases (the Hackworth trap). Now: each run stamps `resultJson.extractorVersion`; the reprocess gate treats a run from an OLDER version as STALE → re-extracts. **BUMP `EXTRACTOR_VERSION` on every chart-extract logic change.** NOT folded into `computeTriggerHash` (that would wedge every case at 'extracting'). Currently v2 = deterministic grant authority.
- **No silent draft failures** (`pipelinePhase.summarizeForOperator`): a paused draft now names the specific failed phase + its recorded reason (PHASE_PLAIN map) as the `operatorMessage`, instead of the generic "we've paused this for a closer look" that hid ~14 distinct failures as one. The manifest already recorded the reason; it was being discarded. (Deploys with the drafter image.)
- **Chart-readiness hash-drift SELF-HEAL + idempotent intake summary (2026-06-26, Dick CLM-29FCC3F1AB / Mittge CLM-E13B746273).** "Send to Drafter" stuck on "Preparing the chart for drafting…" for 15-20 min on cases that were 100% extracted. Root: `intakes.ts` ALWAYS minted a fresh `Intake_Summary.pdf`; a re-assign/retry created a SECOND one whose `ObjectCreated→ocr-start` added a `read` row AFTER extraction completed, drifting `computeTriggerHash` so no run matched → `deriveChartBuildState` returns its `'extracting'` DEAD-END (`chart-build-state.ts:168`) forever (the stuck-run watcher is blind to a *zero-run* drift; it only reaps runs that exist). Two fixes: (1) `intakes.ts` skips the mint when the case already has an `Intake_Summary.pdf` (one summary per case, stable hash); (2) the `GET /chart-readiness` route SELF-HEALS — when state is `'extracting'` with NO run matching the current hash, it auto-calls `maybeEnqueueChartExtract` (its own rate-guard makes this idempotent) and logs `chart_build_stalled` LOUD for a CloudWatch alarm. Recovery for the two live wedged cases was `POST /internal/cases/:id/force-extract`. (Deploys with the api Lambda.)
- **SC-STATUS PROVENANCE / source-authority (Woodley fix, 2026-06-26 round 6 — DARK, flag `SC_PROVENANCE_ENFORCED=off`).** The chart-extraction LLM read a VETERAN-AUTHORED goal-doc ("30% — Recurrent esophageal stricture, DC 7203" = what he's TARGETING) and wrote it into `sc_conditions` as a real grant (`service_connected`, ratingPct 30); Ask-Aegis + the drafter then argued from that false premise. Only the VA grants service connection. Fix: classify each SC row's SOURCE DOCUMENT authority (`sc-authority.ts` `ScAuthorityTier`: va_decision/va_summary = authoritative; clinical/veteran_or_lay/unknown = not) DETERMINISTICALLY (the AI can't be trusted with the highest-stakes field — same rationale as the deterministic grant floor) via docType + a filename/content fingerprint; stamp `sc_conditions.source_authority_tier` + `sc_status_authoritative` (2 additive nullable columns, migration `20260726000000`). The KEYSTONE gate `case-framing.buildGrantedScAnchors` routes the strict `service_connected` filter through `effectiveScStatus` so a non-authoritative extracted grant is EXCLUDED from the granted anchors (the drafter can't build a secondary theory on a fake primary); the merge (`chart-merge-apply`) downgrades a non-authoritative grant → `pending` at WRITE time (catches both the model AND the deterministic grant extractor). `claimed_unverified` is a DERIVED value (never stored). MANUAL (RN-typed) rows always trusted. DARK BY DEFAULT: flag off ⇒ `effectiveScStatus`/merge are byte-identical pass-throughs (tier stamped + logged only); flag on ⇒ demotion fires + a `sc_status_downgraded_on_extract` log (alarm-ready). Worker gains `reservedConcurrentExecutions:8` (re-extraction wave guard). The Woodley regression test proves the fake grant is excluded when enforcing. Flag lives on BOTH the api (consumer gate) + worker (merge). **FLIP (Ryan's call — needs cost sign-off): bump `EXTRACTOR_VERSION` 2→3 + set `sc_provenance_enforced=on` together + run a throttled active-case backfill (~$30-150).** DEFERRED follow-ups (not yet built): the extraction-prompt `scFraming` field (upstream belt-and-suspenders; the deterministic merge-gate already closes Woodley); Ask-Aegis `chartSlice` "claimed, not confirmed" annotation; the Overview provenance chip + "request the VA rating codesheet" flag; the FRN `framingGate.js` legacy-`granted_service_connections` down-weight. 3-agent scope (ai-sme + architect + aws) + 18 sc-authority tests.
- **DOCTOR PACK ("abridged notes") — guarantee the 5 evidence categories (2026-06-26, EMR api Lambda; allocator behind flag `DOCTOR_PACK_CATEGORY_FLOORS`, default OFF/DARK).** Dr. Kasky: the abridged notes "suck" — they didn't reliably surface (1) pertinent clinical pages, (2) the VA SC-list page, (3) the denial page(s), (4) the defining study with its value (AHI/labs), (5) lay/veteran statements. 3-agent diagnosis: `PACK_PAGE_BUDGET=15` was the de-facto selector (selector finds 67-277 pertinent pages, budget guillotines to 15); the category soft-caps + floors summed to exactly 15 so the `'other'` bucket — where mis-classified SC-list/study/denial docs land — starved to 0; the content classifier read only OCR pages 1-2 + had NO patterns for rated_disabilities_view/benefit_summary/sleep_study/audiogram/PFT/progress_notes (so those → `unspecified` → `'other'` → evicted); a denial letter even mis-tagged `dd_214`; the clinical floor didn't protect whole-doc-passthrough entries (0 clinical pages on a real case) and the `NO_CLINICAL_DX` warning miscounted pre-budget. FIX (two lanes): **classifier** (`key-docs-classifier.ts`, `CLASSIFIER_VERSION→1.3.0/num 3`) — add the 6 missing content patterns (phrase-anchored, ordered after higher-value siblings), reorder `dd_214` after the denial block, classify on whole-doc (8 pages × 1500 chars, `doctor-pack-generate.buildContentHintText`), filename tie-break for explicitly-named files; page-picker (`doctor-pack-page-llm.ts`, `PAGE_LLM_VERSION→1.1.0`) `PAGE_TEXT_CHARS 600→1200` + value-page targeting prompt + audiogram/PFT in `LLM_PICKER_DOCTYPES`. **allocator** (`doctor-pack.ts`/`doctor-pack-generate.ts`/`doctor-pack-grounded-pages.ts`, flag-gated) — per-category FLOORS (presence-gated, not back-fillable), budget→30 (`PACK_PAGE_BUDGET_CATEGORY_FLOORS`), chart-fact category override (`chartFactCategoryByDocument`: granted SC→sc_proof, denied→denial, problem→clinical) so a mis-classified doc still reaches the right floor, condition→study force-include (`CONDITION_STUDY_DOCTYPES`), post-budget `missingClinical` + per-category DROPPED warnings, and a cover-index "Required evidence checklist" asserting each of the 5 categories present-with-pages or NOT FOUND/NONE-ON-FILE. Flag OFF = byte-identical (clinical-only floor, no override, 15-budget). `DOCTOR_PACK_GROUNDED_PAGES` already live `on`. Doctor-pack generates in the API Lambda only; ships via `cdk deploy --all`; regen via the "Regenerate abridged notes" button or `POST /cases/:CaseId/doctor-pack/generate`. tsc 0, 157 tests green. ROLLOUT: deploy dark → flip `DOCTOR_PACK_CATEGORY_FLOORS=on` + bump `CLASSIFIER_VERSION` reclassify → regen the 3 validation cases. (Security: the hand-rolled `frn-diag-readonly-zimmelman` Lambda carrying the plaintext master DB password was deleted; RDS credential rotation is a reported follow-up.)
- **ADVISORY PERSONA — aggravation-first parenthetical retired → evidence-led (2026-06-30, EMR api Lambda; `backend/src/advisory/systemPrompt.ts` + vendored `intent_recipes.json`).** Aligns the cloud "Ask AI about this case" advisory with the drafter framing walk-back (794ec6e / CLAUDE.md Step 1b): replaced "select framing MECHANISM-first (aggravation-first among sound anchors)" with "no preset framing priority — direct/secondary/aggravation/presumptive equal footing; the theory follows the evidence; raise aggravation only where the record supports it." MECHANISM-first gating + the BVA-atlas-internal-only guard (Rule 17) preserved. The retired local-dashboard `chatService.js` carried a louder copy of the same doctrine but is DEAD code (poller-only host, not RN-reachable). 1-line change in each of the 2 files; JSON valid; stale phrases grep-clean.
- **DOCTOR PACK — calm clickable table-of-contents cover (2026-06-27, EMR api Lambda + assembler Lambda; flag `DOCTOR_PACK_LINKED_COVER`, default OFF/DARK, flag-OFF byte-identical).** Dr. Kasky-approved redesign of the pack's first page: replaces the plain-text cover-index dump (case id + theory + per-doc "p36, p41-43" page-range soup + a "pinned" why-line dump + a raw filename in every row) with a **CALM clickable contents page** — a short title block (`Doctor Pack` / `<vet> · <caseId>` / `Claimed:` / `Theory: <plain prose>`), a **2-3 line case snapshot** (`Service-connected: <cond pct · …>` / `Active problems: <dx · …>`, truncated `+N more`), then **contents grouped by the five evidence categories** (CLINICAL / SERVICE-CONNECTION PROOF / PRIOR DENIAL / DEFINING STUDY / LAY EVIDENCE) where each row is `<friendly filename-free label> — <short why> … p. <N>` with ONE page ref, and a missing must-have category shows a calm `Not found in chart` / `None on file`. **Architecture — TS owns rects, Python owns offsets:** `doctor-pack-generate.ts buildCoverIndexLines` (now returns `{lines, contentRows}`) builds the calm layout + `coverFriendlyLabel` (NEVER a filename, unspecified→category-derived); `record-text-render.ts` gains `previewRecordTextLineRects` (replays the renderer's exact y-walk over the deterministic layout constants to give each rendered line a PDF-space rect) so the generate path maps each content row → its rectangle and emits a **`coverLinkMap`** (`{entryIndex, coverPageIndex, rect, category, categoryLabel, label}`, entryIndex = MANIFEST entry index with the cover as #0) onto `manifestJson`. The **Python assembler** (`assemble.py`, extracted PURE from `handler.py`) captures `entry_start_page[i] = len(writer.pages)` BEFORE appending entry i (so the offset reflects the framing/cover pages + whole-doc clamp + 60-cap), then **fail-open** stamps a `pypdf` `Link(rect, target_page_index=entry_start_page[entryIndex])` per row + a 2-level outline (category → doc). Flag OFF (or no content rows) ⇒ no `coverLinkMap` ⇒ the assembler skips link/outline ⇒ today's behavior; the cover text is byte-identical to the pre-flag plain cover (pinned why-lines + categoryAssertions checklist preserved on the OFF path). The cover's own page count is resolved by a $0 in-memory measure pass so the printed `p. N` is the predicted assembled page; the clickable link is authoritative regardless. Tests: TS `doctor-pack-linked-cover.test.ts` (layout + friendly labels + one-ref-per-row + rects) + the existing `golden-pack-selection.test.ts` ROUND-2/PR-3 blocks now pin flag-OFF byte-identity; Python `test_assemble.py` (entry_start_page under clamp+cap, one /Link per row → resolved /Dest == entry_start_page, 2-level outline, fail-open on absent/malformed map). Backend 2622 green + tsc 0; assembler pytest 6 green. ROLLOUT: dark-deploy → regenerate a real pack → flip `doctor_pack_linked_cover: "on"`. Prior plain-text cover + the dead WeasyPrint cover/TOC logged in the SUPERSEDED table.
- **DOCTOR PACK — floors-before-pinned + per-category pinned cap + A&P-preferred clinical (2026-06-27, EMR api Lambda; same flag `DOCTOR_PACK_CATEGORY_FLOORS`, flag-OFF byte-identical).** A multi-SC Blue Button (every grant/rating page back-mapped as a PINNED grounded page) starved the clinical + lay floors to ZERO → `NO_CLINICAL_DX` (Thon). Root cause: in `applyPackPageBudget` (`doctor-pack.ts`) the **Phase-0 pinned reservation ran FIRST and uncapped**, so the sc_proof pins drained the whole 30-page budget before the per-category floor loop was reached. **Fix A (load-bearing):** the pinned reservation is extracted into a `reservePinned({perCategoryCap})` closure; the **flag-ON branch runs the per-category FLOOR loop FIRST, then `reservePinned`, then the soft-cap phase** (floors-before-pinned). The flag-OFF branch keeps `reservePinned` FIRST (uncapped) → byte-identical. The floor take is count-based and emission reconciles count vs the specific reserved pinned pages via `takePinnedAware`, so a floor satisfied by prefix-count still keeps its doc's pinned page. **Fix B (companion):** inside `reservePinned`, flag-ON only, a slot is SKIPPED when `categoryTaken(cat) >= CATEGORY_SOFT_CAPS_WITH_FLOORS[cat]` (sc_proof=8) — so one pinned-heavy category cannot drain the budget during reservation and starve the others; `categoryTaken` already reflects the Fix-A floor pages, so pinned tops a category to its cap and no further; `'other'` (no soft cap) is uncapped/defensive. NOTE: the cap is a CATEGORY budget — it can be fully satisfied by ONE 10-page grant doc, so lower-ranked sc_proof docs in the same category legitimately drop (a per-doc-fairness round-robin was NOT in scope). **Fix C:** A&P-section-preferred clinical pages — `page-selector.ts` gains an optional pure `preferAssessmentPlan?: boolean` on `PageSelectorInput` (the module never reads env; `doctor-pack-generate.ts` sets it from `categoryFloorsEnabled()` at the `selectPages` call site, the same path `narrowOutOfLlmScope` routes bulk Blue Button clinical to). In the `progress_notes`/`blue_button` branch, when enabled AND any selected page carries an A&P marker (`/assessment (and|&|/) plan/i`, `^assessment:`, `^plan:`, `A/P`, `impression:`), the kept set NARROWS to the A&P page(s) — Dr. Kasky "just the A&P usually" — and the dx is GUARANTEED to survive (a condition-mentioning page is retained if no A&P page mentions the condition); NO A&P marker anywhere ⇒ falls back to the full condition/recent set (never drops the dx). Flag OFF ⇒ prop absent ⇒ byte-identical. Tests: RED→GREEN drain repro + END-phase floor invariant (`doctor-pack.test.ts`) + A&P narrow / dx-retain / unsectioned-fallback / flag-absent (`page-selector.test.ts`); full backend suite 2607 green, tsc 0. NO existing test changed (the reorder only affects the flag-ON + pinned combination, which no prior test exercised). Prior pinned-FIRST ordering logged in the SUPERSEDED table.
- **SC-PROVENANCE round 7b — CONSERVATIVE demotion after the live dry-run (2026-06-26, EMR api Lambda; still flag-DARK).** The live dry-run (round 7, below) on the last 12 real cases showed the original "demote anything not provably authoritative" would strip **31 grants incl. real VA-rated conditions** (Sleep Apnea 50%, migraine 30%, knee 30%, combined 80/90%) that only landed as tier `unknown`/`clinical` because their source is an image/"Misc" file the classifier can't fingerprint — and `overFilterWatch=0` was falsely reassuring (its letterhead heuristic can't read OCR'd images). Ryan chose CONSERVATIVE. Now demotion keys on the TIER, not the collapsed `scStatusAuthoritative` boolean: `effectiveScStatus` + the merge + the dry-run demote ONLY a PROVEN-junk source (`isProvenNonAuthoritativeTier` = tier `veteran_or_lay`: goal-doc/lay/intake). An UNCONFIRMED grant (`unknown`/`clinical`) is KEPT as service_connected and FLAGGED for verification (`scStatusNeedsVerification`, dry-run `needsVerificationCount` + action `keep_flag_unverified`), NOT stripped. Still kills Woodley (his `.docx` goal-doc = veteran_or_lay). `sourceAuthorityTier` threaded through `ScConditionInput`/`case-framing-stamp`/`sc-reconcile` to the keystone gate. Re-validated live: on this data the demote set collapses from 31 → just the goal-doc/intake rows; the image-sourced grants are kept+flagged. Tests updated (44 green: sc-provenance-dryrun + sc-authority + framing + stamp). DEFERRED: the RN-facing "claimed — verify vs VA codesheet" UI surface for the kept+flagged set (the data is now stamped; the chip is not built).
- **SC-PROVENANCE round 7 — safe incremental flip + deterministic re-classification dry-run/apply (2026-06-26, EMR api Lambda; still flag-DARK).** Two changes that make the round-6 flip safe to do incrementally + actually validatable. (1) **`effectiveScStatus` now TRUSTS legacy-null:** demotion fires ONLY on an AFFIRMATIVE non-authoritative tag (`scStatusAuthoritative===false`); a legacy/pre-fix extracted row (`null`) is trusted until re-classified. So flipping `SC_PROVENANCE_ENFORCED=on` is a behavioral no-op for every row already in the DB (the old "null demotes" would have stripped granted anchors fleet-wide from not-yet-classified cases). (2) **The CLEANING PATH is a deterministic backfill, NOT re-extraction** — `planMerge` protects prior-extracted rows so re-running the extractor SKIPS existing SC rows and never re-classifies them; the merge's WRITE-time downgrade only bites new inserts. New `sc-provenance-dryrun.ts` + `GET /internal/admin/sc-provenance-dryrun` re-derive each extracted SC row's tier from its source `Document` (filename + docTag + a first-2-page **text sample** — the OVER-FILTER DEFENSE: a real VA rating decision whose docTag is `unspecified`/filename generic is rescued to `va_decision` by the letterhead+decision-recital fingerprint). DRY-RUN (default) is SELECT-only: per-row report + an **OVER-FILTER WATCH** list (wouldDemote rows whose text still looks like a real decision → eyeball) + `blindDemoteCount` (no text sample) + `noSourceExtractedCount`. APPLY (`apply=true`, requires EXPLICIT `caseIds` — never fleet-wide) mirrors the writer in a txn: non-authoritative `service_connected` → `pending` + `ratingPct`/`grantedDate` null + stamp tier; idempotent (no-op re-stamps skipped, no version churn); logs `sc_status_backfill_downgraded`; refreshes derived stamps per case. Token-gated (whole `/internal` router). Tests: sc-provenance-dryrun (8, incl. Woodley demote + the real-VA-decision RESCUE) + sc-authority (18). Architect note carried: 4 gate-BYPASS consumers (`draft-readiness.ts:513`, `soap-context-assembler.ts:114`, `internal-worker.ts:947`, `strategy-preview.ts` fail-open) read raw `status==='service_connected'` — all no-ops on legacy-null; the apply's `status→pending` write cleans them for free. **Flip still owed (Ryan's call): dry-run last-5 + Woodley → eyeball OVER-FILTER WATCH → apply on validated caseIds → set `sc_provenance_enforced=on` (both stacks) + add the `sc_status_downgraded_on_extract`/`sc_status_backfill_downgraded` metric-filter alarm.** 2-agent QA (aws scope + architect route review = SAFE-TO-DEPLOY-DARK).
- **P0-1 no-block-draft demotion (drafter `cf54613-routepicker`, FRN cf54613, 2026-06-26 round 5).** Once a draft EXISTS it must always be viewable/editable/forwardable; a transient crash in a POST-draft advisory phase was parking it. Key finding: the publish gate is NOT the re-block — the panel-presence linters (`adversary_review_missing` etc.) are stale-UNBYPASSABLE (NOT in `artifactPublisher.ESSENTIAL_RULE_IDS`, the only live blocker, and not run on the cloud path at all), so the only thing parking a good draft was `fail()` itself. DEMOTED to advisory-continue (draft exists): adversary crash-after-retry, specialist crash-after-retry, refine-loop crash / ok:false, qa_report crash → synthesize a safe zero-finding result, write an honest `{status:'unavailable'}` sidecar (auditable proof the phase didn't run), defer a physician advisory (flushed into the FINAL grade after the grader; null floor → does NOT lower the probative grade), continue. The presence linters short-circuit on the sentinel to ONE `*_review_unavailable` warning (physician_decision, never blocks) instead of N role-skip criticals. STAY fatal (architect-confirmed; the worklist wrongly listed these): pre-draft index_consult/framing_gate (genuinely-no-draft) + render crash / render_parity (no artifact / wrong-content integrity). c0 grade-A byte-identical (demote branches dead on happy path). Two architect-QA passes (design + post-code = SHIP). See FRN INCIDENTS.md + `reference_artifactpublisher_two_blocklists.md`. NEW EMR follow-up (not this image): surface the `*_unavailable` advisory + degraded-phase note prominently in OpsHeldPanel / the grade UI.
- **§VIII orphan-prune (drafter `27967ef-routepicker`, FRN 27967ef, 2026-06-26 round 4).** Dr. Kasky on Sanderson nephroptosis: §VIII references not cited in the body + a body cite (Wadström) missing from §VIII. Root cause: the §VIII list (LLM-authored) and the body prose are grown by separate mechanisms that never reconcile — the floor-4 backfill (`citationFallback.backfillSectionViiiCitations`) appends grounded PMIDs to §VIII ONLY (never the body) to hit a count, manufacturing orphans (Boyan "crossed renal ectopia" = wrong condition + Yoshida, neither cited). Fix: `sectionAssembler.pruneOrphanCitationsFromSectionVIII` — the complement of the forward reconcile. Removes a §VIII entry only when it has a PMID, is NOT a model-authored scored anchor (backfilled or unscored), and is cited nowhere in §I..§VII (PMID or any author surname). Provenance-scoped + fail-safe keep-on-doubt; a clean letter is byte-identical. Wired last in `run-letter-pipeline.js` (backfill→strip→reconcile→**prune**→advisory). Proven on real v33: §VIII `[Gürel, Boyan, Yoshida]` → `[Gürel, Wadström]`; byte-identical no-op on golden grade-A lumbar + Apolito. Deterministic-only (no drafter-prompt change → no c0 needed; $0 proof on real artifacts). See FRN INCIDENTS.md 2026-06-26.
- **Citation DEPTH (drafter 094c672) + SOAP truncation + revision-preview modal (2026-06-26 round 3).**
  - **Citation depth (drafter image `094c672-routepicker`, FRN 094c672, c0 grade A):** the live-trace forensic showed letters still thin. Three fixes: (1) `assessCoverage` (step0-index-consult.js) now counts PMID-bearing CO-LOCATED typed anchors (mechanism/physiology + epi + ≥4 usable) within a ~900-char window of the disease term, NOT raw library-text richness — so "2 UpToDate, 0 PMID" reads INSUFFICIENT and fetches PubMed; (2) `reconcileSelectedCitationsIntoSectionVIII` (sectionAssembler.js) promotes every selected∧pmid∧used-in-prose citation into the §VIII numbered list (Sanderson's dropped Wadström physiology PMID), no network/zero-fabrication; (3) the §VIII floor backfill never silent (console + `v<N>_citation_backfill.json` sidecar + sub-floor advisory) and runs **MODE B** (Dr. Kasky chose try-harder): broaden the NCBI cascade (synonym/MeSH + physiology lane via `broaden`) + a `fitsGraduated` gate that accepts a rare/structural physiology paper without an occupational token, off-etiology/PAPA guard intact. Flip with `FRN_CITATION_BACKFILL_MODE=A`.
  - **SOAP truncation (soap-overview.ts):** `clamp()` was hard-slicing each section mid-sentence (Carr CKD). Now cuts at a sentence boundary + raised limits (subj 1600 / obj 2200 / asmt 2800 / plan 1200) + `SOAP_MAX_TOKENS` 2400→3600.
  - **Revision-preview expand modal (RevisionPreviewModal.tsx):** reusable ~2/3-screen modal with Accept/Decline/Close, wired into GuidedRevisionPanel (revised-letter preview) + CitationEnricherPanel (per-candidate), both RN + physician views.
- **SOAP serve-stored-first / OSA heading / cover-memo controls / silent-failure alarms (2026-06-26 round 2).**
  - **SOAP serve-stored despite hash drift (Bays deep fix):** `decideServeStored` (`soap-overview.ts`) + a serve-first branch in `case-viability.ts` (the `/soap-overview` route) now SERVE the persisted current-shape grounded note (`fallback:false`, schema v27) on a plain open regardless of route-picker inputHash drift, and fire ONE background refresh if the fingerprint drifted — instead of regenerating a truncated ungrounded fallback. The route-picker VERDICT keeps its strict hash gate (decoupled from serving the note). Kills the perpetual "couldn't be generated" banner + the "regenerate" nag on `status='drafting'` cases. (hash-drift gate wedge class — see the FRN memory.)
  - **OSA heading from the picker theory (one-brain #72/#89):** `soapHeadline.ts` gains `routePickerCardFraming` (the ready route-picker card's chosen framing), placed above the static strategy engine, so the Case Overview heading follows the chosen theory (depression) not the stale intake claim ("secondary to Knee"). `SoapOverviewCard.tsx` feeds `aiState.card.lead.framing`.
  - **Cover-memo staff controls (Spring):** new `Case.coverMemoSuppressed` + `coverMemoTextOverride` (migration `20260723000000_cover_memo_controls`, additive). `delivery.composeMemo` now (a) derives the memo condition LABEL from the route-picker plan (`lead.claimed`) not the broad intake bucket, (b) drops the memo when suppressed, (c) uses a staff text override (guarding the `[SIGNATURE]` anchor). New routes `POST /delivery/cover-memo/suppress`, `PUT/DELETE /delivery/cover-memo`; `DeliveryPanel` gains a do-not-send checkbox + edit/revert.
  - **Silent-failure alarms:** `api-stack` `chart_build_stalled` + `drafter-stack` `references_empty` / `all_rejected_off_topic` metric-filter alarms → **ops-alerts** (confirmed gmail sub). `chart_extract_error` (#116) was already live. No drafter image rebuild (filters match strings the live `b2d10e1` already logs). Deploys with api + drafter (cdk) — additive, no task-def/Lambda-code change to the drafter.
- **SOAP banner honesty + precompute observability (2026-06-26, Bays).** The "couldn't be generated / refreshes on the next open" SOAP banner made a false promise on a perpetual-fallback case; reworded to the truth ("brief read derived from the verdict; full summary being prepared in the background; the brief is accurate, just shorter"). `precomputeSoapNoteForCase` now logs the PERSISTED outcome (`soap precompute outcome` grounded/fallback/persisted) so a perpetual-fallback case is observable. The DEEPER fix — serve the stored grounded note on a plain open despite route-picker input-hash drift (`ai-viability.ts:220`, `case-viability.ts`) — is a DEFERRED dedicated cycle (touches the SOAP grounding contract).
- **Render glyph-fold** (`foldRenderable.js`): PDFKit can't render non-CP-1252 glyphs (ș, ≥) → corrupt PDF → render_parity_mismatch on an A-grade letter. Fold to ASCII at every PDF sink + persisted txt. Live (drafter + render image `0fa7a8b`).
- **Body-quality park** (2026-06-21, `routes/drafter.ts` `/halt`): the FRN cloud drafter SKIPS the full publish linter (no `claims` table in Fargate), so ~40 body linters never gated → editorial-meta / fabricated-PMID / dual-prong-missing / SSN-PHI / locked-block / Section III list-format leaks reached the physician. The FRN `draftBodyQualityGate` now PARKS such a (fully-drafted) letter via the EMR `/halt` callback. The `/halt` receiver allowlists `body_quality_critical` (maps to case `needs_rn_decision` + draft_decision `pause`/item `body_quality` — mirrors `verify_error` but labeled honestly, so the chart Decisions log doesn't read as a dx hold). **Detection is dual:** `reasonCode === 'body_quality_critical'` OR `haltGate === 'body_quality'` — because the FRN side currently still emits the allowlisted `verify_error` with `haltGate:'body_quality'` until its drafter image redeploys; BOTH are accepted (`isBodyQualityHalt` in `frontend/src/types/prisma.ts`). The full payload (incl. `materialIds[]` / forthcoming `material[{id,section,detail}]`) persists in `DraftJob.haltPayloadJson`. Cross-repo follow-up owed FRN-side: emit the dedicated `body_quality_critical` code + richer `material` rows once the drafter image redeploys.
  - **HOLDS ARE ADVISORY + EDITABLE, not re-draft-only (2026-06-22, option A — no FRN change):** a body-quality park is the ONE halt class where a FULL letter WAS produced. The FRN drafter does not POST an artifact key on `/halt`, so the `/halt` receiver (`routes/drafter.ts`) RECONSTRUCTS the canonical key `drafter-artifacts/<caseId>/v<N>/v<N>.txt`, validates it (`isDrafterArtifactS3Key`), and **HeadObject-checks it via the injected S3 client**. ONLY when the object actually exists does it (a) persist `DraftJob.artifactTxtS3Key` and (b) **advance `Case.currentVersion` to the halted version** — so `resolveCurrentTxtKey` (DraftJob fallback) + `getLetter` reach the held letter, and the RN can open + fix it in the full editor. When the object is ABSENT — every dx/event verification hold, OR S3/bucket unconfigured — NOTHING about version/key changes: the case stays no-draft so the dx-halt confirm/halt panel is untouched. **Fail-SAFE default: never advance `currentVersion` onto a draft we cannot prove exists.** Gated by the pure classifier `haltShouldCarryDraft(reasonCode, haltGate)` (= `isBodyQualityHalt`'s server twin). Pinned by `backend .../halt-preserves-draft.test.ts` (5 cells) + `halt-classifier.test.ts`.
  - **`needs_rn_decision` is in `EDITABLE_STATUSES`** (`routes/letter.ts`) so the held letter opens + saves in the editor (a hand-fix is cheaper than a ~$15 re-draft). `needs_records` stays OUT (no draft by definition).
  - **§VII OPINION EDITING — physician-only, narrowed holding lock (2026-06-24, Puller; `routes/letter.ts` + `services/letter-opinion-excerpt.ts`).** The old lock froze the ENTIRE Section VII opinion sentence, so a physician couldn't even rephrase causation→aggravation (it 422'd `holding_changed`). Now: (a) the AI-edit lock (`holdingConclusionWeakened`, replaces `holdingChanged` at the surgical-APPLY + guided-PROPOSE call sites) blocks ONLY a weakening/removal of the probability conclusion — a 3-tier strength ordinal STRONG("more likely than not"/>50%) > EQUIPOISE("at least as likely as not") > BELOW; ANY downgrade or removal → 422, but a causal-verb change at equal strength is ALLOWED; (b) a **physician-only role gate** (`sectionViiChanged` → 403 `section_vii_physician_only`, "Section VII can only be edited by a physician; pass edits in a message when submitting for review") fires on ALL THREE edit paths (PUT save, surgical apply, guided propose) and ONLY when §VII content actually changed — `ops_staff`/RN edits to other sections are untouched; (c) the AI proposer prompt (`letter-surgical-propose.ts`) now permits a causation↔aggravation / primary↔secondary rephrase on instruction while keeping the ">50%" conclusion + CFR cite word-for-word. A true theory swap (primary↔secondary across §III/§VI) is still a re-draft, not a surgical edit. Pinned by `letter-holding-lock.test.ts` + `letter-routes.test.ts`. EMR-only (no drafter rebuild).
  - **SAFETY companion — sign-off refuses a parked case:** advancing `currentVersion` onto a held-for-defect letter makes it byte-bindable, and `routes/sign-offs.ts` had NO case-status guard. It now **409s any sign-off while `status ∈ {needs_rn_decision, needs_records}`** (reason `case_parked`) so a physician can never sign a parked-for-defect letter; resolve the hold (fix → `physician_review`) first. Pinned by `sign-offs-routes.test.ts`. (Delivery/approve were already status-gated to `{delivered, paid}` / valid transitions; the status CHIP is driven by `case.status` not `currentVersion`, so a parked case still reads "Needs RN decision".)
  - **FORWARD DOOR — a hold is a SOFT caution, never a trap (2026-06-22, Ryan "see, edit, FORWARDED"):** a held letter the RN fixes by hand must be able to move RN → physician → sign → deliver. Editing via `PUT /letter` creates a new version but does NOT change status, so the park needed an explicit forward edge. `case-status-transitions.ts` now allows **`needs_rn_decision → physician_review`** (role: the default admin/ops_staff — identical to the canonical `rn_review → physician_review` "send to doctor" hop), and `routes/cases.ts` mirrors the **assigned-physician guard** onto it (no unassigned doctor's queue). `needs_records` gets NO such edge (it never produced a draft). The `BodyQualityHoldCard` surfaces a **"Send to doctor for review"** action (shown only when a produced draft exists) that drives this edge. This is the human path the sign-off `case_parked` 409 already instructed ("fix → physician_review") — now wired end-to-end. The truthful-attestation safety is untouched: sign-off still demands honest answers (it gates on ANSWER CONTENT at the doctor, never on blocking forward progress). Pinned by `case-status-transitions.test.ts` + `cases-routes.test.ts` (200 + no-physician 409) + `BodyQualityHoldCard.test.tsx`.
  - **RETURN-TO-PHYSICIAN DOOR — a finalized/signed letter can be sent back to the MD (2026-06-28, Ryan "RN caught an error after sign-off"; WIDENED 2026-06-28 for the physician/owner + paid recall):** once a letter is physician-signed and `status='delivered'` (Ready for delivery / Invoiced) — or even `'paid'` (billed/closed) — if an error is spotted there was no path back (the MD cannot re-sign a sent-back letter, the RN cannot edit, only send). `POST /cases/:id/return-to-physician` (`requireRole admin/ops_staff/physician`) flips `<source> → physician_review` in ONE transaction with a **MANDATORY** non-empty message (the actor's reason), written as a `CaseMessage` and surfaced to the MD on open. This is a **SECOND writer of `physician_review`** (the `/status` route is no longer the only one). **ROLE × STATUS policy (in-handler gating on top of `requireRole`):** returnable SOURCE statuses = `{delivered, paid}` (`RETURNABLE_TO_PHYSICIAN` set); from `delivered` → `{admin, ops_staff, physician}`; from `paid` → `{admin, physician}` ONLY (ops_staff is rejected `403` — an RN cannot reopen a closed/billed case). The physician role was added so **Dr. Kasky (role `physician`, the signer/owner) can recall his OWN signed letter** — previously the button never rendered for a physician and the only `ops_staff` was a deleted test account, so a real `delivered`-but-unpaid case (Shirley Carr, CLM-1018C5A59A) was stuck with no door. **BILLING SAFETY:** returning does NOT auto-un-invoice / un-charge / refund — the source status + `wasPaid`/`wasInvoiced` flags are recorded in the `case_returned_to_physician` activity-log `detailsJson`, and the modal warns ("already been invoiced/paid … does NOT cancel the invoice or refund the payment — handle billing separately") when the letter was billed. The signed letter stays the current version (no re-draft, no byte change → no-block-draft honored). The **generic `/status` route's `delivered`/`paid → physician_review` bare flip stays ADMIN-ONLY** (`case-status-transitions.ts` `requiredRolesForCaseStatusTransition` — the G4 stale-signature lock + the new `paid → physician_review` admin rule); ops_staff/physician reach the recall ONLY through this dedicated, message-mandatory door (which requires AN assigned physician to exist but does NOT yet verify the CALLER is that physician — sufficient for a single-physician/owner; a caller-is-assigned check mirroring `cases.ts:331` is a tracked follow-up before a 2nd physician account is added), never the bare flip. `case-status-transitions.ts` map: `paid` is no longer terminal (`paid: ['physician_review']`). Frontend: `ReturnToPhysicianModal` (mandatory message + billing warning, atomic) + a "Return to physician" button on `CaseDetailPage` shown when `(status==='delivered' && admin/ops/physician) || (status==='paid' && admin/physician)`. Pinned by `cases-routes.test.ts` (10-cell suite incl. the Shirley `delivered+physician→200` + `paid+ops_staff→403`) + `case-status-transitions.test.ts` (paid edge + admin-only).
  - **RN UI:** the `BodyQualityHoldCard` (`Gate2HaltPanel` branch) is now **three actions** when a draft exists — **"Open letter editor"** (advisory amber lead) + **"Send to doctor for review"** (the forward door) + re-draft (secondary); with no draft it is re-draft only. The `drafting`-status interrupted card (`OpsHeldPanel`) drops the misleading hardcoded "Drafting was interrupted" headline for honest copy, and its produced-letter affordance routes to the **editor**, not the read-only PDF (the real reason still renders from `summarizeForOperator`'s `operatorMessage`). Both wired from `CaseDetailPage` via `onOpenEditor → navigate('/cases/:id/letter')`.
  - **PLAIN-LANGUAGE HALT SUMMARY (2026-06-24, Ryan "no codish errors — give me a checklist + a conditional grade + the specific fix"):** the pure helper `frontend/src/lib/draftHaltSummary.ts` (`buildDraftHaltSummary`) turns the already-persisted `DraftJob.manifestSnapshot.phases` + `gradeSidecarJson` into an RN-readable block rendered by `OpsHeldPanel`: a **conditional grade chip** (`Grade: B+ — ready to ship` vs `fix N first`, `(provisional)` when the grade is a synthesized floor), a **6-stage checklist** (17 pipeline phases collapsed to *Wrote the draft / Specialist review panel / Automatic fixes / Quality checks / Graded the letter / Final formatting check* with ✓/✗/○ and the stage where it stopped), a **plain reason** per stopped stage (`plainPhaseReason` — NEVER surfaces a rule_id/stack/`{}`/`threw`/`request_id`; falls back to a per-phase sentence), a **"Fix before sending"** list (normalized `targeted_revision_hints`), and a **single next-action** line. grade/ship are read from `gradeSidecarJson` at runtime (the whole qa_grade object persists there even though its narrow DB type only declares the hints; loose `GradeSidecarLike` makes it assignable). **SAFETY GATE — `summary.shipAsIs`** is the ONE flag the UI keys "ready to ship" on: true ONLY when the halt is purely cosmetic on a ship-grade letter (`crashedPhase ∈ {render,render_parity} && ship==='ship'`) **OR** the run finished clean (no stopped stage) AND the grade is real (not a synthesized floor) AND ship-recommended AND zero fixes. A substantive crash or a floor grade is ALWAYS `shipAsIs:false` — it can never wear the all-good face even if an earlier grader stamped `ship` before a later crash (the leak a 2-agent QA pass caught: door must not trust `ship` in isolation). Pinned by `frontend/src/__tests__/draftHaltSummary.test.ts` (incl. 3 SAFETY cases: floor-grade-with-ship, substantive-crash-with-empty-fixlist, cosmetic-is-the-one-allowed). Pure/presentation-only — no pipeline, gate, or data-flow change.

## 7. 🗄 RETIRED / SUPERSEDED log (append-only — so dropped decisions don't resurface)

| Date | What | Why retired | Replaced by |
|---|---|---|---|
| 2026-06-29 | **TWO deterministic pre-draft VERDICT surfaces on the case-page Action tab** — (1) the `RecommendedPlanCard` verdict chip ("Draft" / "Contact veteran" / "Not supportable") from `recommendedPlan()` (`caseReadinessVerdict.ts`), mounted in the "View full analysis" `<details>`; and (2) the `StrategyPreviewCard` deterministic VERDICT noise — the "Strategy checks (N)" ✓/△/✗/○ `cdsEngine` checklist (incl. "In-service event documented ✗"), the "Thin — review"/"Review needed" tier chip, and the auto-expand-when-concerning behavior | three pre-draft verdict surfaces could DISAGREE on one screen (the SOAP/viability card said supportable while the deterministic chip said "Contact veteran" / a brittle ✗ check flagged a documented-but-differently-worded fact); Dr. Kasky ignores the cautions and new RNs would be in "constant concern". One LLM brain, one signal | **the LLM SOAP/viability card (`SoapOverviewCard`) is the SINGLE pre-draft signal** on the Action tab. `RecommendedPlanCard` is UNMOUNTED from the case page (`CaseDetailPage.tsx` — import + render removed); the component file + its standalone tests are KEPT, and `recommendedPlan()` stays exported (other lanes use it) — do NOT delete. `StrategyPreviewCard` is now a QUIET argument/theory summary: it keeps the plain "Argument:" line, the "Anticipated: stronger as secondary" FYI, the aggravation (3.310(b)) framing, the "Veteran's theory" prose, and the "Computed from N facts" input visibility — but renders NO chip, NO checklist, NO ✓/⚠ verdict. UNTOUCHED: the SOAP card + its provisional/analysis-incomplete banners, the chart-readiness OCR hard-stop (`loadReconciledChartReadiness`), the unreadable-file `ChartRecoveryBanner`, and all factual chips (SC-status colors, RecordsChip, lifecycle, Duplicate). Story-order lock updated (`CaseDetailPage.storyOrder.test.tsx`). |
| 2026-06-29 | **Deterministic pre-draft readiness AUTO-EVALUATION** — the Gate-1 ✓/⚠ "Essential documents missing" cautions (`evaluateDraftReadiness` items/missing/ready + `conditionDocumented`'s exact-canonical dx match, rendered in `Gate1ChecklistModal`) AND the `DRAFT_READINESS_GATE` POST `/draft` hard-block (`drafter.ts`) | the auto-evaluation over-fired — an exact-canonical dx match false-flagged a documented *clinically-equivalent* condition (e.g. claim "Plantar Fasciitis" vs an on-file related foot pathology) as "diagnosis missing"; Dr. Kasky: cautions were wrong ~90% of the time, and a dormant `=on` flag would have re-introduced exactly that hidden hard-block | **a PLAIN RN ATTESTATION** — `Gate1ChecklistModal` is now a human checklist (dx / SC-docs / prior-denial / **the nexus-judgment call**), every radio starts UNSET, no machine ✓/⚠, no readiness fetch. Drafting is gated by the human completing the checklist, not a string-match. The LLM SOAP note is the analysis surface. The chart-readiness OCR door (`loadReconciledChartReadiness`) is SEPARATE and still enforced. `getDraftReadiness`/`evaluateDraftReadiness` are KEPT (intact, tests green) ONLY to serve the `caseFraming`/`buildState` provenance `DecisionsOverridesPanel` reads — their essential-docs `items` are now inert output; do NOT re-wire them into a gate. |
| 2026-06-28 | **Chip-LOCK-only (2026-06-27, `soapChip.ts`):** lock the Overview/SOAP chip color to the persisted `note.action` | the lock was correct but the underlying `note.action` was still (a) generated at temperature 0.5/1.0 (non-deterministic), (b) force-overwritten on every precompute, and (c) re-rolled by a recompute storm during drafting — so the chip still wobbled amber→green→amber even while reading a single stable field | **decide the verdict once and make it STICK**: temperature:0 on both decision LLM calls + `reconcileStickyAction` (ungrounded recompute can't overwrite the action) + freeze the auto-fired recompute while `status='drafting'`. The chip read stays locked to `note.action`; now `note.action` itself is stable. See the §6b chip-stability entry. |
| 2026-06-27 | **Doctor-pack WeasyPrint HTML→PDF cover + TOC pages** (`handler.py _render_cover_page` / `_render_toc_page`, the `DOCTOR_PACK_WEASYPRINT_LAYER_ARN` optional-layer branch + `_non_content_page_count` accounting) AND the **plain-text cover-index dump** (`buildCoverIndexLines` legacy layout: `Case <id> — claimed condition` + `Theory:` + per-doc `pX-Y` page-range soup + `pinned <why>` lines + a raw filename in every row) | WeasyPrint was NEVER attached on the live path (`No module named 'weasyprint'` on 100% of runs) — it was dead code that never rendered; the plain-text cover read like a debug dump (filenames, page-range soup, no navigation) | **ONE pdf-lib cover** (the calm clickable table-of-contents — `buildLinkedCoverLines`, behind `DOCTOR_PACK_LINKED_COVER`) + the assembler's PURE `assemble.py` (merge + `coverLinkMap` PDF links + 2-level outline, fail-open). `handler.py` is now thin I/O only. WeasyPrint code deleted outright; the `DOCTOR_PACK_WEASYPRINT_LAYER_ARN` synth hook in `workers-stack.ts` is now an inert no-op (left in place to avoid CDK diff noise, scheduled for removal). See the §6 doctor-pack hard-rule entry. |
| 2026-06-19 | **Static M/E (mechanism/evidence) anchor ratings on the Overview card** ("M=4,E=2") | brittle static table, didn't match drafter judgment | the **AI route-picker** (`deriveAiViability`) — one brain for card + drafter + Ask-Aegis |
| 2026-06-19 | The `/cases/:id/soap-overview` 2nd-LLM-call endpoint | 29s timeout silent-fail | SOAP card renders deterministically from the shared `viability-card` query (one LLM call) |
| 2026-06-19 | Synchronous picker compute on GET `/viability-card` | ~22-25s call can't fit the 29s cap → silent fail | async self-invoke compute off the request path + persisted-plan read (see §5) |
| 2026-06-20 | Deterministic string-assembled SOAP "note" on the Overview card | read like a dump (S echoed in A, O = a wall of every SC condition) | AI-synthesized SOAP note (`buildSoapNote`, Sonnet) — smooth S/O/A/P; the deterministic verdict stays the fail-open fallback |
| 2026-06-22 | **`caseReadinessVerdict` (deterministic engine) OWNING the Overview chip** | the chip ("Not supportable") could CONTRADICT the SOAP note's Assessment/Plan ("supportable / proceed to draft") — two brains on one card | **the chip is now a PROJECTION of the AI route-picker viability band** (`routePickerBandToVerdict`, ONE brain — same plan the SOAP renders + the drafter pleads). The deterministic engine is DEMOTED to **fallback-only**: it drives the chip ONLY when there is no ready route-picker plan (flag off / cold / error, i.e. `routePickerViability == null`) and can never contradict a ready plan. When the band says supportable but the deterministic core would Stop, the band WINS the headline and the core's concern is surfaced as a VISIBLE `band_vs_deterministic` disagreement (no silent flip). `routePickerBandToVerdict` must agree band-for-band on go/no-go with `soap-action-map.ts planViabilityToAction` (pinned by `frontend .../oneBrainChip.agreement.test.ts`). |
| 2026-06-22 | SOAP sync read building its `SoapContext` from the FE-POSTed body on the ungrounded branch | the sync-read fingerprint then differed from the async precompute's (which uses the assembler) → the precomputed note was NEVER found → permanent fallback note on every open (incl. hard refresh) | **BOTH sync branches build ctx via `assembleSoapContextForCase` (server-derived only; FE body no longer feeds the fingerprint)**, so write==read and the precomputed note serves for $0. `precomputeSoapNoteForCase` now also persists an UNGROUNDED note (framing=null) so cold cases have a served note. `SOAP_NOTE_SCHEMA_VERSION` 25→26 (pre-fix stored notes invalidate). |
| 2026-06-22 | SOAP coverage note computing coverage with EMPTY file-read-statuses | every page counted as unread → SOAP Objective said "0% of pages read" while the chart chip read 100% (two readers of one report disagreed) | **one shared `loadExtractionCoverageForCase(db, caseId)`** (in `extraction-coverage.ts`) used by BOTH GET `/cases/:id/extraction-coverage` AND the SOAP assembler — identical inputs (real `file_read_status` rows via `isEffectivelyRead` + latest run + per-page provenance), so coverage can never drift. |
| 2026-06-23 | The chart-extraction card's single "Chart extraction: 100% Complete" headline | it CONFLATED two phases — OCR ("pages read") and the SEMANTIC extract ("chart analysis") — so a chart whose pages were all OCR'd but whose analysis FAILED/was-interrupted read as "100% Complete", hiding a failed analysis that the SOAP verdict was then built on | **TWO-STAGE coverage SSOT** in `extraction-coverage.ts` (`ExtractionCoverage.pagesRead` + `.chartAnalysis`, both derived from the same fields): the card renders two plain-English lines ("Pages read: 100% (28 of 28)" / "Chart analysis: ✓ Complete (N findings)" \| "⚠ didn't finish — retry"), and the SOAP Overview reads `chartAnalysis.state` — when not `complete` it shows a PROMINENT top banner (names the likely-cause large file) and renders the verdict as **provisional**, never a confident conclusion on an empty/partial chart. One object feeds card + banner so they can't disagree. The FE card is DEFENSIVE (derives the two stages from `status`/`coveragePct` when an older payload lacks them). |
| 2026-06-27 | **Doctor-pack Phase-0 PINNED reservation running FIRST + uncapped** (`applyPackPageBudget`, flag `DOCTOR_PACK_CATEGORY_FLOORS` ON) | a multi-SC Blue Button whose every grant page was a pinned grounded page drained the entire 30-page budget before the per-category floor loop ran → clinical + lay floors = 0 → `NO_CLINICAL_DX` (Thon) | **floors-before-pinned + per-category pinned cap** — flag-ON branch runs the floor loop FIRST, then `reservePinned({perCategoryCap:true})` (skips a slot once `categoryTaken(cat) >= CATEGORY_SOFT_CAPS_WITH_FLOORS[cat]`), then the soft-cap phase. Flag-OFF keeps pinned-FIRST uncapped (`reservePinned({perCategoryCap:false})`) → byte-identical. Companion: `page-selector.ts preferAssessmentPlan` narrows clinical pages to the A&P section when the flag is on. See the §6 doctor-pack hard-rule entry. |
| 2026-06-23 | Editor `WarningList` surfacing all save-time sanity findings + `meta_canonical` bare-word `/\bcanonical\b/` leak rule | the RN saw cosmetic style lines (em-dash/jargon/sentence-variance/banned-word) on every save → trained to ignore the panel; `meta_canonical` false-positived on legit "canonical mechanism/pathway" medical prose | **WarningList keeps only meaningful rules** (`placeholder_token_introduced`, `locked_block_corrupted`; editorial/meta leaks stay surfaced via the separate non-blocking `letter.leaks` block). **`meta_canonical` tightened** to fire only when "canonical" modifies an editing object (`canonical (Section <roman> )?(format\|template\|language\|structure\|…)`) — the directive shape of the real Apolito/Zodrow leaks, which the conditional/locked-template/restructure rules independently still catch. |
| 2026-06-23 | The two-stage honesty layer above was COSMETIC-only: the SOAP banner + "(provisional)" label changed, but the VERDICT itself stayed confident on a failed/incomplete analysis, and a brand-new/empty case (no run yet) cry-wolfed "didn't finish" | a physician could still read a confident "Not supportable" built on an empty chart (the Herman harm), and new cases falsely alarmed | **(1)** `ChartAnalysisState` gains **`not_analyzed`** — runStatus `null` or zero chart inputs → resting state, NOT `incomplete`; never fires the banner/provisional/cause-file. `likelyCauseFile` gated to `failed`/`incomplete` only. SSOT invariant enforced (a `status==='complete'` object may carry only `complete`/`not_analyzed`; the builder throws otherwise). **(2)** `caseReadinessVerdict` now feeds `chartAnalysisState` into the VERDICT: `failed` → suppresses the directional text, renders the new `analysis_failed` verdict ("re-run to assess"); `incomplete`/`in_progress` → downgrades a directional/negative verdict to `read_chart_first` + lowers confidence; **FAIL-SAFE** — when the coverage query is loading/errored (state unknown, parent sets `chartAnalysisUnknown`) the verdict goes provisional, never confident. A found bridge route is never clobbered. **(3)** whole-file gaps (`unread`/`needs_manual_summary`) also raise the provisional banner. **(4)** banner restyled LOUDER than ambient amber (red/orange 6px rule + ring + filled "!" chip). **(5)** `meta_canonical` widened to allow ≤2 (incl. hyphenated/comma'd) intervening words so "canonical letter format"/"canonical, well-recognized format" are caught while mechanism/pathway/presentation still pass. |
| 2026-06-30 | The chart-extraction card read "Pages read 100% (0 of 0)" + "Chart analysis: Not analyzed yet" for a MANUALLY-added case (New-Claim, no uploaded files — records come from the veteran's prior case) → "0 of 0 = 100%" read as broken (Ryan/Hackworth) | coverage counts DOCUMENT rows; a manual case has zero, so it borrowed the empty-new-case wording | **`ExtractionCoveragePanel.tsx` manual-entry display branch**: `isManualEntry = cov.totalFiles === 0` (FILE count, not page count — a case with files but uncounted pages must NOT match) replaces the two stage lines with one neutral line "Manual entry — records from the last case are pulled in automatically. Upload additional records as needed." + a neutral "Manual entry" chip; ≥1 file → the normal "N of M" count, unchanged. Pure FE display branch, no new `chartAnalysis` state, SSOT invariant untouched. **`totalFiles === 0` is the canonical "manual entry / no uploaded files" display signal.** |

### OWED (Ryan-approved, 2026-06-20 — see handoff `20260620_soap_note_ai_synthesis_and_deploy_blocker.md`)
- **Precompute the SOAP note on chart-extract FINISH** so it's ready when the chart opens (no 10-15s wait) —
  fire at the tail of the internal-worker merge handler via the async self-invoke pattern; persist; chart-open
  POST stays the backstop.
- **Anti-confabulation guards**: a deterministic number/measurement/date verifier (every value in the note
  must trace to the source facts) + a cheap short second LLM grounding pass (async, fail-open). These GATE
  collapsing the dense panels.
- **Collapse the dense panels** (Background & argument / Anchor viability / Recommended plan) behind a
  "View details ▾" toggle once the guards are in; keep them as the API-down fallback.
  - DONE/SUPERSEDED 2026-06-29: the surviving panel (Background & argument = `StrategyPreviewCard`) lives in
    the "View full analysis" `<details>` and was de-noised to a quiet argument/theory summary; the Anchor-viability
    (M/E) card and the `RecommendedPlanCard` verdict were REMOVED from the page (not just collapsed). The SOAP card
    is the single pre-draft signal. See the §7 SUPERSEDED rows (2026-06-19, 2026-06-29).
| 2026-06-16 | Textract-only OCR as the primary scanned-page reader | dropped combo-page handwriting (false "100% read") | per-page Sonnet vision worker + honest per-page coverage |
| (legacy) | Local Node/SQLite dashboard as production EMR | moved to AWS | this EMR (`compact-emr-work`); localhost retains only the inbound Gmail poller |

> When you retire a design, ADD A ROW here in the same commit. Include any dead code still in the tree.

---

## 8. Maintenance

- **Same-commit rule** (above) — enforced socially now; a pre-commit/END linter that fails when a diff
  touches `_ENABLED|_MODEL|claude-|new Anthropic|schemaVersion` without staging this file is a TODO.
- **Staleness:** re-review when a brain/flag/flow changes; refresh the "Last reviewed" line + commit.
- Cross-links: FRN `ARCHITECTURE.md` (drafter internals, website, Stripe/Gmail/R2 chains), `DEPLOY.md`
  (CDK stacks + deploy commands), `INCIDENTS.md` (postmortems — Secrets partial-ARN, Jotform TZ, NUL-byte).
