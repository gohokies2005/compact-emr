# This Lambda deploys via Code.fromAsset with NO bundling (workers-stack.ts) — deps here are
# DOCUMENTATION ONLY. Every non-boto3 dep must be pip-installed (vendored) INTO this directory
# with the Lambda platform pinned, per workers/README.md "Vendored dependencies":
#   pip install <pkg> -t . --platform manylinux2014_x86_64 --only-binary=:all: --python-version 3.12
# boto3 is runtime-provided (not vendored).
boto3>=1.34,<2.0
pypdf>=6.0,<7.0  # vendored (pure Python); 6.13.2 is the copy in this dir
# pycryptodome: pypdf's AES/RC4 crypt provider — VA PDFs commonly ship AES-encrypted with an empty
# user password, and without a crypto provider pypdf raises DependencyError on open (Kimbrough
# CLM-41E9900FB8, 2026-07). Vendored as a manylinux abi3 wheel (Crypto/ with .so binaries).
pycryptodome>=3.23,<4.0
