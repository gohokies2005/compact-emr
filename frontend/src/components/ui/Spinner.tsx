export function Spinner({ label, small = false }: { readonly label?: string; readonly small?: boolean }) { return <span className="inline-flex items-center gap-2"><span className={`${small ? 'h-4 w-4' : 'h-5 w-5'} animate-spin rounded-full border-2 border-current border-t-transparent`} aria-hidden="true" />{label ? <span className="text-sm text-slate-500">{label}</span> : null}</span>; }
